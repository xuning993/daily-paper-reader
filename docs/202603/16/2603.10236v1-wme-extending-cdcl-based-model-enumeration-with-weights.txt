Title: WME: Extending CDCL-based Model Enumeration with Weights

URL Source: https://arxiv.org/pdf/2603.10236v1

Published Time: Thu, 12 Mar 2026 00:15:03 GMT

Number of Pages: 18

Markdown Content:
# WME: Extending CDCL-based Model Enumeration with Weights 

# Giuseppe Spallitta #

Rice University, Houston, Texas, United States of America 

# Moshe Y. Vardi #

Rice University, Houston, Texas, United States of America 

Abstract 

In this work we investigate Weighted Model Enumeration (WME): given a Boolean formula and a weight function over its satisfying assignments, enumerate models while accounting for their weights. This setting supports weight-driven queries, such as producing the top-k models or all models above a threshold. While related to AllSAT, Weighted Model Counting, and MaxSAT, these paradigms do not treat selective enumeration under weights as a native solver task. We present CDCL-based algorithms for WME that integrate weight propagation, weight-based pruning, and weight-aware conflict analysis into both chronological and non-chronological backtracking frameworks. Chronological backtracking exploits implicit blocking and keeps the clause database compact, thereby reducing memory footprint and enabling efficient propagation. In contrast, non-chronological backtracking with clause learning supports explicit blocking and restarts. We show that both approaches are feasible and complementary, highlighting trade-offs in pruning effectiveness with weights and clarifying when each performs best. This work establishes WME as a solver-level reasoning task and provides a systematic exploration of its algorithmic foundations. 

2012 ACM Subject Classification Theory of computation → Automated reasoning; Mathematics of computing → Enumeration; Theory of computation → Algorithm design techniques 

Keywords and phrases AllSAT, Weighted Model Enumeration, Weighted Conflict Analysis 

Supplementary Material Software (Source Code) : https://github.com/giuspek/WME 

# 1 Introduction 

In many artificial-intelligence applications, including probabilistic inference [ 20 ], structured decision-making [ 22 ], fault diagnosis [ 7 ], and interpretable machine learning [ 33 ], the objective is not merely to obtain a satisfying assignment of a Boolean formula, but to identify a set of "high-quality" models. Such models often correspond to the most probable explanations under a weight function. Classical formulations such as Maximum a Posteriori (MAP) [ 42 ] and Most Probable Explanation (MPE) [36, 8] capture this idea by seeking the single highest-scoring assignment. Yet in practice, many applications require richer outputs: multiple explanations, such as top-k weighted models or all models above a weight threshold, are indispensable for robust decision support and interpretability. This need has long been recognized in database theory through top-k query processing [19, 14] and in probabilistic inference through top-k

explanation tasks [44]. Existing CDCL-based reasoning tools fall short of providing native support for weight-aware enumeration of Boolean formulae. AllSAT solvers [ 41 , 21 , 15 , 39 ] enumerate indis-criminately, ignoring weights entirely. Weighted Model Counting (WMC) [ 34 , 30 , 18 , 13 , 11 ]computes global aggregates but provides no access to individual weighted models. MaxSAT solvers [ 10 , 28 , 16 ] return a single maximum-weight solution, but lack mechanisms for ranking or filtering multiple alternatives. The 2020 MaxSAT Evaluation introduced a dedicated track for top-k enumeration [ 2], highlighting both the recognized importance and the unsolved nature of top-k reasoning. 

> arXiv:2603.10236v1 [cs.LO] 10 Mar 2026

2 WME: Extending CDCL-based Model Enumeration with Weights 

In summary, existing CDCL-based approaches either enumerate without regard to weights, aggregate weights without revealing individual models, or return only a single optimum. However, solver-native support for weighted model enumeration remains limited. As a result, a natural and practically important task remains unexplored: Weighted Model Enumeration 

(WME). This work formalizes and develops a CDCL-native framework for WME and lays out the algorithmic foundations for its study. Although the problem is naturally connected to both AllSAT and MaxSAT, addressing it efficiently requires solver mechanisms that go beyond a straightforward combination of the two. Beyond the definition, we introduce weight-aware propagation and pruning rules that enable the solver to prune infeasible branches early, and weight-conflict clauses that integrate weight reasoning into CDCL’s clause-learning algorithms. These mechanisms reshape the solver’s behavior, enabling both threshold-based and top-k enumeration to be performed natively. A central issue, then, is how weight conflicts are resolved. In classical SAT solving, non-chronological backtracking and state-of-the-art CDCL techniques are considered indispensable for efficiently scanning the search space [ 23 ], and their benefits have been discussed for enumeration [ 41 ]. Yet their advantages may not carry over when weight thresholds guide the search. Conversely, chronological backtracking, recently shown to be effective for model enumeration [ 39 , 40 ], could align more naturally with WME. This duality raises the question of which backtracking approach is better suited for weight-aware enumeration, motivating the development and empirical comparison of both chronological and non-chronological CDCL frameworks for WME. 

# 1.1 Contributions 

We formally introduce Weighted Model Enumeration , a generalization of classical reasoning problems, such as AllSAT, WMC, and MaxSAT, supporting the enumeration of weighted satisfying assignments, possibly under weight-based criteria. We extend CDCL with weight-aware propagation and pruning, enabling the solver to detect weight conflicts when no completion of the current trail satisfies the weight constraints. This introduces new forms of conflict analysis and clause learning. We develop two CDCL frameworks for WME: a chronological variant (implicit blocking clauses, low memory footprint, order-sensitive) and a non-chronological variant (explicit blocking clauses, restart-friendly). We implement both designs and study their behavior across thresholds and weight distributions, highlighting regimes where one approach outperforms the other. 

# 1.2 Related Work 

AllSAT solvers such as clasp [ 15 ], the set of tools BC , NBC and BDD from [ 41 ], and, more recently, AllSATCC [ 21 ] and TabularAllSAT [39 , 40 ] enumerate all satisfying assignments without regard to weight. Introducing weighted and filtered results based on weight constraints must be done post hoc, which is computationally prohibitive in large solution spaces. 

WMC tools such as D4 [18 ], Cachet [34 ], miniC2D [30 ], ADDMC [12 ], DPMC [13 ], sharpSAT-TD 

[17 ] and algebraic extensions [ 11 ] compute aggregate weights over all models. These are central in probabilistic inference but do not enumerate or rank individual assignments. 

MaxSAT solvers including MaxHS [10 ], Open-WBO [ 24 ], MPE-SAT [ 36 ], and the newer 

Open-WBO-Inc [16 ] and TT-Open-WBO-Inc [28 ] optimize for a single maximum-weight solution. While multiple high-quality models can, in principle, be obtained by repeated MaxSAT calls G. Spallitta and M.Y. Vardi 3

with additional exclusion constraints, this is not the native objective of MaxSAT solving and does not directly provide threshold-based or incremental ranked enumeration. The MaxSAT Evaluation’s experimental top-k track [ 2] further illustrates that ranked extraction is a distinct challenge rather than a standard MaxSAT operation. A possible approach to target WME is to compile the formula into a tractable representa-tion from knowledge compilation [ 9] (e.g., d-DNNF and related decision-diagram formalisms) and then enumerate solutions in ranked order. Recent work started investigating the idea of ranked enumeration on circuits under restricted scoring schemes [ 1, 26 ]. This direction could be effective when compilation is feasible and results in a compact representation. However, our focus in this manuscript is different: we target anytime, solver-native top-

k/threshold enumeration directly within CDCL-style search. This is already a natural fit in the propositional setting, since the weight bounds can be maintained online during the search and used immediately for pruning. The choice is also motivated by how naturally it carries over to SMT. In that setting, a purely propositional compiled representation is not enough: an assignment may satisfy the Boolean abstraction while still being theory-inconsistent. Therefore, a compilation-based approach would need to account not only for propositional structure, but also for the effect of theory reasoning and theory-pruned branches, which is technically nontrivial [ 25 ]. By contrast, CDCL( T ) [ 29 ] already combines Boolean search with online theory consistency checking, making the integration of weight reasoning into the search loop more straightforward. An alternative related line of work is the dynamic-programming optimization tool DPO 

[31 ]. DPO targets optimization on hybrid constraint systems (including XORs) by combining SAT reasoning with dynamic programming over the input problem structure. While DPO 

supports computing an optimal high-weight model, its optimization framework does not allow a trivial modification to enumerate multiple high-weight satisfying models. 

# 2 Weighted Model Enumeration 

Let F be a CNF Boolean formula over variables V = {A1, . . . , A n}. A total assignment 

η : V → {⊥ , ⊤} satisfies F if all clauses of F evaluate to true under η. A partial assignment 

μ assigns only a subset of the variables in V. We represent assignments as sets of literals, e.g. μ = {A1, ¬A2, A 3}, or as conjunctions of literals, e.g. μ = ( A1 ∧ ¬ A2 ∧ A3), using the two representations interchangeably. In the CDCL loop, μ denotes the current trail, i.e., an ordered partial assignment partitioned into decision levels. Let ε denote the empty trail. We define a weight function w : L → R>0 over the literal set L = {Ai, ¬Ai | Ai ∈ V} . Finally, let vars (μ) = {var (ℓ) | ℓ ∈ μ}, where var (ℓ) denotes the Boolean variable underlying literal ℓ.

▶ Definition 1 (Weighted Model Enumeration) . Given a Boolean formula F over variables V

and a literal-level weight function w : L → R>0, the Weighted Model Enumeration (WME) 

problem asks to compute the set of weighted satisfying assignments: 

Mw(F ) = {(η, w (η)) | η |= F } where w(η) = ∏

> ℓ∈η

w(ℓ).

The goal is to enumerate every model η satisfying F together with its aggregated weight 

w(η). The weighted model enumeration problem generalizes: 

AllSAT : when w(ℓ) = 1 for all ℓ, we get the unweighted set of satisfying assignments; 

WMC : when only the scalar quantity ∑ 

> η|=F

w(η) is returned, rather than the full set of weighted models. 4 WME: Extending CDCL-based Model Enumeration with Weights                                                                    

> A1A2A3Sat. FWeight A1A2A3Sat. FWeight
> 000×–100×–001×–101✓0.6·0.2·0.5 = 0 .06
> 010×–110×–011✓0.4·0.8·0.5 = 0 .16 111✓0.6·0.8·0.5 = 0 .24
> Table 1 All possible assignments of F= ( A1∨A2)∧A3and their associated weights.

To illustrate the WME task, consider a simple weighted CNF instance over Boolean variables A1, A2, and A3:

F = ( A1 ∨ A2) ∧ A3        

> A1A2A3
> w(ℓ)0.6 0.8 0.5
> w(¬ℓ)0.4 0.2 0.5

Table 1 lists all 8 possible assignments, indicating which ones satisfy F , and reports their corresponding weights for those that do. In addition to the baseline WME task, we define two constrained variants: 

Threshold WME : Given a threshold θ ∈ R>0, enumerate all models η |= F such that 

w(η) ≥ θ1.

Top-k WME : Enumerate the k models with the highest weights among all models in F .These two modes are general enough to express a wide range of practical requirements, including top-k explanation, utility-based model extraction, and probabilistic inference. 

▶ Remark 2. Throughout this work, we adopt the multiplicative aggregation of weights, 

w(η) = ∏

> ℓ∈η

w(ℓ),

as commonly used in Weighted Model Counting and probabilistic reasoning. While the examples in this paper use weights in (0 , 1] , in line with the probabilistic intuition common in WMC, the upper-bound reasoning developed later applies over the entire positive range. For numerical stability, one may equivalently work in the log domain by replacing products with sums. This is often preferable in floating-point implementations, as repeated multiplication of many positive weights can easily cause underflow, overflow, or loss of precision. 

# 2.1 Comparison to Existing Approaches 

Table 2 positions Weighted Model Enumeration with respect to the main SAT-based paradigms it relates to, namely AllSAT, Weighted Model Counting, and MaxSAT. While these approaches share individual aspects with WME (such as model enumeration, weight handling, or optimization objectives), none of them combine these features within a single solver framework. AllSAT [ 41 , 21 , 40 ] provides complete model enumeration but lacks any notion of weights or ranking, making selective exploration infeasible without exhaustive enumeration. WMC        

> 1We focus on lower-bound thresholds w(η)≥θin this paper. Other relational operators (e.g., >,≤,
> <) can be handled analogously by reversing the comparison and/or using corresponding upper/lower residual bounds.

G. Spallitta and M.Y. Vardi 5                 

> Feature AllSAT WMC MaxSAT WME
> Model enumeration ✓××✓
> Weight support ×✓✓✓
> Top-k/threshold enumeration ××∼✓2✓
> Table 2 Comparison of WME against similar SAT-based paradigms.

[12 , 13 , 11 ] supports weights but collapses all models into a single aggregate quantity, offering no access to individual solutions. MaxSAT [ 24 , 28 ] optimizes for a single best model and does not support ranked or threshold-based enumeration without repeated solver invocations. In contrast, WME integrates model enumeration, weight-aware search, and weight-based pruning directly into a CDCL-style solver. This enables native support for top-k and threshold-based enumeration with incremental output, allowing entire regions of the search space to be pruned based on weight bounds when needed. 

# 3 Integrating Weight Reasoning into CDCL-Based Frameworks 

Weighted Model Enumeration extends standard CDCL-based enumeration by incorporating Boolean search with weight-based reasoning and threshold constraints, while preserving the overall structure of the CDCL loop. In particular, Boolean constraint propagation and Boolean clause learning remain unchanged. The key extension is the integration of weight-based conflict analysis into the CDCL loop. In addition to detecting logical conflicts, the solver continuously evaluates whether the current partial assignment can still satisfy an active weight bound θ, which may represent either a fixed threshold or a dynamically updated cutoff (e.g., the current k-th best score in top-k enumeration). If the maximum achievable weight of a branch falls below θ, the branch is declared infeasible, and a weight conflict is analyzed, resulting in a learned clause that prunes the corresponding region of the search space. Conceptually, this mechanism plays a role analogous to theory-conflict detection in lazy SMT solvers [ 3], but operates over simple weight bounds rather than complex theory constraints. 

# 3.1 Weight Propagation and Pruning 

WME extends CDCL search with a numerical dimension: besides logical consistency, the solver must track whether the current partial assignment can still be extended to a model whose weight reaches the active threshold θ. The goal is to detect as early as possible when this is no longer feasible. This process, called weight-based pruning , complements standard Boolean propagation and provides the condition that triggers weight conflicts. Each literal ℓ

is associated with a real-valued weight w(ℓ). For every Boolean variable Ai, we define: 

best (Ai) = max {w(Ai), w (¬Ai)}

For each partial assignment μ, the solver maintains two quantities: the partial weight w(μ) on the assigned variables, and an optimistic residual bound Imax (μ) on the unassigned variables: 

Imax (μ) := ∏

> A∈V\ vars( μ)

best (A). 

> 2Some MaxSAT extensions can retrieve multiple near-optimal models via repeated invocations, but this remains an indirect use of MaxSAT rather than a native ranked-enumeration operation.

6 WME: Extending CDCL-based Model Enumeration with Weights  

> Algorithm 1 Weight-State Maintenance and Pruning

Require: Current assignment μ 

> 1:

w(μ) ← 1 ▷ partial assignment weight  

> 2:

Imax (μ) ← ∏ 

> A∈V

best (A) ▷ optimistic residual  

> 3:

function Assign (μ, ℓ ) 

> 4:

μ.append( ℓ) 

> 5:

w(μ) ← w(μ) · w(ℓ) 

> 6:

Imax (μ) ← Imax (μ)/best (ℓ) 

> 7:

function Unassign (μ, ℓ ) 

> 8:

μ.remove( ℓ) 

> 9:

w(μ) ← w(μ)/w (ℓ) 

> 10:

Imax (μ) ← Imax (μ) · best (ℓ) 

> 11:

function WeightConflict (μ) 

> 12:

if w(μ) · Imax (μ) < θ then  

> 13:

return true ▷ prune branch  

> 14:

return false 

Algorithm 1 maintains these quantities incrementally. At initialization (lines 1–2), w(μ) is set to 1, and Imax (μ) is the product of all best (Ai), corresponding to the assumption that unassigned variables will take their highest-weight polarity. When assigning a literal ℓ (lines 3– 6), the solver multiplies w(μ) by w(ℓ) and removes the corresponding best (Ai) contribution from Imax (μ). Unassigning a literal (lines 7–10) reverses both updates. Both operations preserve the invariant 

∀η ⊇ μ : w(η) ≤ w(μ) · Imax (μ).

That is, w(μ) · Imax (μ) is an upper bound on the weight achievable by any completion of μ.For brevity, we write best (ℓ) to denote best (var( ℓ)) .The pruning test (lines 11–14) is invoked after Boolean propagation. If w(μ) · Imax (μ) < θ ,even an ideal extension cannot reach the lower bound. In this case, we prune the branch, and a weight conflict can be reported 3.

▶ Proposition 3 (Weight pruning soundness) . Let μ be any partial trail reachable by the main CDCL loop, and let w(μ) and Imax (μ) be maintained as in Algorithm 1. Then: 

∀η ⊇ μ : w(η) ≤ w(μ) · Imax (μ).

In particular, if WeightConflict (μ) returns true , i.e., w(μ) · Imax (μ) < θ , then no completion η ⊇ μ can satisfy w(η) ≥ θ.

Proof. We prove that w(μ) · Imax (μ) is always an upper bound on the weight of any extension of the current trail μ.   

> 3A symmetric test based on the pessimistic bound Imin could prune branches that exceed an upper bound, though, for clarity, only the lower-bound variant is shown here.

G. Spallitta and M.Y. Vardi 7

Initially μ = ε, w(μ) = 1 , and Imax (μ) = ∏ 

> A∈V

best (A), so the bound holds trivially. Whenever a literal ℓ of variable A is assigned, Algorithm 1 updates w(μ) ← w(μ) · w(ℓ) and 

Imax (μ) ← Imax (μ)/best (ℓ), thus maintaining: 

w(μ) = ∏

> ℓ∈μ

w(ℓ) and Imax (μ) = ∏

> B∈V\ vars( μ)

best (B).

Let η ⊇ μ be any completion. For every unassigned variable B, the literal ℓB chosen in η

satisfies w(ℓB ) ≤ best (B) by definition. Thus: 

w(η) = ∏

> ℓ∈μ

w(ℓ) · ∏

> B∈V\ vars( μ)

w(ℓB ) ≤ ∏

> ℓ∈μ

w(ℓ) · ∏

> B∈V\ vars( μ)

best (B) = w(μ) · Imax (μ).

Hence w(μ) · Imax (μ) is a valid upper bound for all extensions of μ, and if w(μ) · Imax (μ) < θ 

then w(η) < θ for every η ⊇ μ, establishing the correctness of pruning. ◀▶ Example 4. Consider the following weighted instance, where ϕ is a subformula over the set of variables B1, · · · Bn:

F = ( A1 ∨ ϕ) ∧ A2.               

> A1A2Bi∈ϕw(ℓ)0.6 0.3 βi∈(0 ,1)
> w(¬ℓ)0.4 0.7 1−βi

Suppose we enforce a lower-bound threshold θ = 0 .2. Initially, 

Imax (μ) = max(0 .4, 0.6) · max(0 .7, 0.3) ·

> n

∏

> i=1

best (Bi)

︸ ︷︷ ︸

> α

= 0 .42 α. (1) Notice that due to all variables in ϕ having weights in the range (0 , 1) , α is guaranteed to be in the same range. Now suppose the trail is μ = {¬ A1, A 2}. Then w(μ) = 0 .4 · 0.3 = 0 .12 .We divide best (A1) and best (A2) from Imax (μ), leaving the residual: 

Imax (μ) = Imax (μ)

best (A1) · best (A2) = 0.6 · 0.7 · α

0.6 · 0.7 = α

. Hence, for every extension of the partial assignment (including the optimistic completion): 

w(μ) · Imax (μ) = 0 .12 · α ≤ 0.12 < θ = 0 .2,

Therefore, this branch can be pruned early, avoiding the assignment of any variables in ϕ.

# 3.2 Weight Conflict Analysis 

The previous section defines how a partial trail maintains an upper bound on the achievable model weight and how branches that cannot reach the target threshold θ are pruned. When such pruning occurs, we interpret it as a weight conflict , analogous to a Boolean conflict in standard CDCL. The goal of weight-conflict analysis is to extract from the current trail a short explanation for why the threshold cannot be met, and to generate a clause that prevents the solver from revisiting the same infeasible region. 8 WME: Extending CDCL-based Model Enumeration with Weights  

> Algorithm 2 AnalyzeWeightConflict (μ)

Require: Current trail μ; threshold θ 

> 1:

S ← GreedyConflictSet (μ, θ ) 

> 2:

Cw ← ∨ 

> ℓ∈S

¬ℓ 

> 3:

Learn (Cw) ▷ add learned weight-conflict clause  

> 4:

Backjump (Cw) ▷ backjump per CDCL policy  

> Algorithm 3 GreedyConflictSet (μ, θ )

Require: Current trail μ; threshold θ 

> 1:

S ← ∅  

> 2:

w(S) ← 1 

> 3:

Imax (S) ← ∏ 

> A∈V

best (A) 

> 4:

L ← literals of μ sorted by ascending w(ℓ) 

> 5:

for each ℓ ∈ L do  

> 6:

S ← S ∪ { ℓ} 

> 7:

w(S) ← w(S) · w(ℓ) 

> 8:

Imax (S) ← Imax (S)/best (ℓ) 

> 9:

if w(S) · Imax (S) < θ then  

> 10:

return S 

> 11:

return S ▷ All literals in μ needed 

▶ Definition 5 (Weight conflict) . Given a CNF formula F , a threshold θ, and a partial trail μ, we say that μ induces a weight conflict if every total extension η ⊇ μ fails to reach the threshold, i.e., 

∀η ⊇ μ : w(η) < θ. 

By Proposition 3, w(μ) · Imax (μ) < θ provides an efficiently checkable sufficient condition: if even the optimistic upper bound on any extension of μ is below θ, the branch can be pruned. 

When a weight conflict is detected (line 11 of Algorithm 1), the solver triggers weight-conflict analysis (Algorithm 2). The procedure first extracts a weight-conflict set S ⊆ μ and then learns the corresponding weight-conflict clause 

Cw := ∨

> ℓ∈S

¬ℓ, 

which blocks the simultaneous reoccurrence of all literals in S.The extraction of the weight conflict set is shown in Algorithm 3. We use a greedy extraction strategy because it is easy to integrate, incurs little overhead, and provides a practical trade-off between clause compactness and extraction cost. Literals in the current trail μ are sorted by ascending weight w(ℓ) (line 4). The procedure then greedily builds a set S ⊆ μ (lines 5–10), while maintaining ( i) the partial weight w(S) = ∏ 

> ℓ∈S

w(ℓ) and (ii ) an optimistic residual bound Imax (S) for the variables not yet fixed by S. As soon as 

w(S) · Imax (S) < θ (line 9), the set S is used to generate Cw.

▶ Proposition 6 (Soundness of greedy weight-conflict learning) . Let μ be a trail such that 

WeightConflict (μ) holds. Let S ⊆ μ be the set returned by GreedyConflictSet (μ, θ ),and let Cw be the learned weight-conflict clause. Assume that when the procedure returns S it satisfies w(S) · Imax (S) < θ , where w(S) = ∏ 

> ℓ∈S

w(ℓ) and Imax (S) = ∏ 

> A∈V\ vars( S)

best (A)G. Spallitta and M.Y. Vardi 9

are maintained as in Algorithm 3. Then every total model η of F with w(η) ≥ θ satisfies Cw.Consequently, learning Cw cannot eliminate any model of weight at least θ.

Proof. Let S be the set returned by GreedyConflictSet . By assumption, at return time the procedure has w(S) · Imax (S) < θ .We first show that this implies that every completion of S is below the threshold. Let 

η ⊇ S be any total assignment containing all literals in S. For each variable A / ∈ vars (S), let 

ℓA be the literal on A chosen by η. By definition of best we have w(ℓA) ≤ best (A), hence 

w(η) = 

( ∏ 

> ℓ∈S

w(ℓ)

)

·

( ∏

> A∈V\ vars( S)

w(ℓA)

)

≤ w(S)·

( ∏

> A∈V\ vars( S)

best (A)

)

= w(S)·Imax (S) < θ. 

Therefore: 

∀η ⊇ S : w(η) < θ. (⋆)Now consider any total assignment η of F that falsifies Cw = ∨ 

> ℓ∈S

¬ℓ. Falsifying Cw

means ℓ is true in η for all ℓ ∈ S, hence S ⊆ η. Applying (⋆) returns w(η) < θ .Thus, no assignment of weight at least θ can falsify Cw. Equivalently, every model η with 

w(η) ≥ θ satisfies Cw, so learning Cw cannot eliminate any model of weight at least θ. ◀▶ Example 7. Consider the following weighted CNF instance: 

F = ( A1 ∨ A2 ∨ ¬ A3).        

> A1A2A3
> w(ℓ)0.6 0.8 0.7
> w(¬ℓ)0.4 0.2 0.3

Suppose the lower-bound threshold is θ = 0 .2 and the current trail is μ = {¬ A1, A 2, ¬A3}.The partial weight is w(μ) = 0 .4 · 0.8 · 0.3 = 0 .096 . Since w(μ) < θ , this branch can be pruned due to a weight conflict (i.e., it violates the weight bound). The greedy strategy identifies the literal with the lowest contribution (here, ¬A3) as the main cause of the underweight condition (since w(¬A3) · best (A1) · best (A2) = 0 .144 < 0.2), returning the weight conflict set S = {¬ A3}. The corresponding weight conflict blocking clause Cw = A3 is then learned. This procedure prevents revisiting assignments containing ¬A3, since no extension would get a score higher than the threshold. 

# 3.3 Residual-Aware Backtracking for Top-k Enumeration 

In top-k enumeration, whenever we find a satisfying assignment whose weight enters the current top-k, we tighten the active threshold θ accordingly. After such an update, continuing the search from the last decision level is often pointless: the current trail μ may already be so constrained that, even under the most optimistic completion of the remaining variables, no model strictly above θ can be reached. Algorithm 4 uses the same optimistic bound from Algorithm 1 to determine how far to backtrack immediately after updating θ. Starting from the current trail μ, the procedure repeatedly removes the most recent literal, updating w(μ) and Imax (μ) according to the incremental rules of Algorithm 1. After each removal, the quantity w(μ) · Imax (μ) remains an upper bound on the weight of any completion of the current trail. Any trail satisfying 

w(μ) · Imax (μ) ≤ θ cannot lead to an improved model, so we continue backtracking. The procedure stops as soon as w(μ) · Imax (μ) > θ , because some completion may still improve on the current cutoff. Thus, the solver can resume the search from the corresponding 10 WME: Extending CDCL-based Model Enumeration with Weights  

> Algorithm 4 Residual-Aware Backtrack (μ, θ )

Require: Current trail μ; maintained w(μ), Imax (μ); threshold θ 

> 1:

while μ contains a literal at decision level > 0 do  

> 2:

ℓ ← μ. pop()  

> 3:

w(μ) ← w(μ)/w (ℓ) 

> 4:

Imax (μ) ← Imax (μ) · best (ℓ) 

> 5:

if w(μ) · Imax (μ) > θ then  

> 6:

return μ ▷ resume search from this trail and decision level  

> 7:

return μ ▷ return the decision level-0 trail and stop enumeration decision level by exploring the alternative branch. If the procedure backtracks to decision level 0 and the bound is still not above θ, then no model of weight greater than θ exists, and top-k enumeration can terminate. 

▶ Example 8. Consider the following weighted CNF instance: 

F = ( A1 ∨ A2) ∧ A3.        

> A1A2A3
> w(ℓ)0.6 0.8 0.7
> w(¬ℓ)0.4 0.2 0.3

The assignment η = {¬ A1, A 2, A 3} satisfies F with w(η) = 0 .4 · 0.8 · 0.7 = 0 .224 . We assume the CDCL trail has the same order. Assuming we are looking for the top-1 solution, the threshold θ is updated accordingly. Removing A3 with ¬A1, A 2 fixed results in: 

(A3 = ⊤) : 0.4 · 0.8 · 0.7 = 0 .224; (A3 = ⊥) : 0.4 · 0.8 · 0.3 = 0 .096 .

No extension of the partial assignment can be a new top-1 solution, so we can prune A3.Removing A2 with only ¬A1 fixed results in: 

(A2=⊤, A 3=⊤) : 0.4 · 0.8 · 0.7 = 0 .224; (A2=⊥, A 3=⊤) : 0.4 · 0.2 · 0.7 = 0 .056; (A2=⊤, A 3=⊥) : 0.4 · 0.8 · 0.3 = 0 .096; (A2=⊥, A 3=⊥) : 0.4 · 0.2 · 0.3 = 0 .024 .

None is above θ, so A2 can be safely removed. At this point, the remaining trail is 

μ = {¬ A1}. Its optimistic residual bound is Imax (μ) = 0 .6 · 0.8 · 0.7 = 0 .336 , which is > θ , so there is a chance that an assignment with a higher score satisfying F does exist. The solver backtracks and flips A1.

# 3.4 Weight-based Variable Priority for Top-k Enumeration 

Many real-world problems are not naturally given in CNF and are first encoded using structure-preserving encodings such as Tseitin [43] or Plaisted–Greenbaum [32]. The newly introduced variables in these encodings (and, more generally, any variable whose two polarities contribute the same weight) are often weight-irrelevant : they do not affect model ranking under a weight-based threshold. Let V be the full variable set. We partition V into weight-relevant variables Wr and weight-irrelevant variables Wi. Accordingly, any assignment η can be decomposed as η = ηr ∪ ηi

with ηr over Wr and ηi over Wi. The contribution of Wi is constant, denoted θi, so we maintain a lower-bound threshold θr on the restricted score over Wr only. The branching heuristic prioritizes variables in Wr so that weight-relevant variables are assigned first. G. Spallitta and M.Y. Vardi 11 

For any assignment ηr over Wr , the assignment weight is defined such that: 

∀ ηi over Wi : w(ηr ∧ ηi) = w(ηr ) · θi

Once all variables in Wr have been assigned, if w(ηr ) < θ r , then no extension over Wi can reach the current threshold. During partial search over Wr , the same reasoning applies using an optimistic residual bound over the still-unassigned variables in Wr .If all variables in Wr are assigned and w(ηr ) ≥ θr , the solver checks extendability to a total satisfying assignment including variables in Wi. When this succeeds, θr is updated accordingly, and residual-aware backtracking is applied with respect to the last decision on a variable in Wr ; assignments on Wi made afterward cannot improve the score. Conversely, if the optimistic bound over the remaining variables in Wr drops below θr , then variables in 

Wi are skipped, and weight-based conflict analysis is triggered. 

▶ Example 9. Consider the following weighted CNF instance: 

F = ( A1∨A2) ∧ A3 ∧ (¬Z1∨A1) ∧ (¬Z2∨A2),Wr = {A1, A 2, A 3}, Wi = {Z1, Z 2}.              

> A1A2A3Z1Z2
> w(ℓ)0.9 0.8 0.7 0.5 0.5
> w(¬ℓ)0.1 0.2 0.3 0.5 0.5

Since both polarities of each Zj carry the same weight, every assignment on Wi contributes the same factor θi = 0 .25 , so Wi is weight-irrelevant. We maintain a lower-bound threshold 

θr over Wr and prioritize decisions on Wr . Assume θr = 0 .1.

Case 1: Suppose the trail on Wr is ηr = {A1, ¬A2, A 3}. Then 

w(ηr ) = 0 .9 · 0.2 · 0.7 = 0 .126 .

Since w(ηr ) ≥ θr , we check extendability to a total model satisfying F (e.g., Z1 = Z2 = ⊥

works). Any completion over Wi preserves the relative ranking of two assignments over Wr ,so if w(μ1r ) > w (μ2r ) then 

w(η1) = w(η1r ) · θi > w (η2r ) · θi = w(η2).

After validating satisfiability, θr is updated with the full score and residual-aware backtracking resumes from the last decision in Wr ; assignments to (Z1, Z 2) are irrelevant for the score. 

Case 2: Suppose instead we reach η′ 

> r

= {¬ A1, ¬A2, A 3}, giving 

w(η′ 

> r

) = 0 .1 · 0.2 · 0.7 = 0 .014 .

Since w(η′ 

> r

) < θ r , the solver can skip assigning Z1, Z 2 and trigger a weight conflict. In the top-k setting, two total assignments may share the same total weight such that: (i) they have the same assignment on Wr , and ( ii ) different assignments on Wi. To avoid pruning such tie-equivalent solutions too early, we activate the optimization only after the first k satisfying assignments have been collected. From that point onward, only assignments whose best achievable score is strictly better than the current k-th score can affect the maintained top-k set. 

# 4 Chronological vs. Non-Chronological Backtracking 

Integrating weight reasoning into CDCL-based enumeration gives rise to two distinct solver designs, depending on how conflicts (both Boolean and weight-based) are resolved. The 12 WME: Extending CDCL-based Model Enumeration with Weights 

first adopts the classical non-chronological clause-learning paradigm of modern SAT solvers [41 , 21 ]. The second combines CDCL with chronological backtracking as used in recent AllSAT engines [ 41 , 40 ]. These two strategies have been explored in the non-weighted setting [39, 40], but weight-based pruning could accentuate their trade-offs in the weighted case. 

# 4.1 Chronological Backtracking Framework 

In the chronological framework, conflicts trigger chronological backtracking, but the solvers still incorporate CDCL-style conflict analysis [ 27 , 39 ]. To maintain chronological traversal, analysis is carried out up to the last unique implication point (UIP), instead of performing the usual first-UIP backjumping [ 39 ]. Blocking previously explored models is implicit : once a branch is closed, the chronological traversal ensures it will not be revisited [27]. The main advantage of this approach is its low memory footprint and fast propagation. Nevertheless, the performance of this design depends heavily on decision order. Early poor branching choices can delay the discovery of high-weight models, reducing the effectiveness of pruning. Restarts would break the implicit blocking guarantee, so they must be disabled [ 39 ], which further increases sensitivity to branching heuristics. In practice, this variant performs best when the solution space is dense, such that implicit blocking remains efficient. 

# 4.2 Non-Chronological Backtracking Framework 

In the non-chronological framework, the solver backjumps to the first UIP rather than the last, thereby returning shorter clauses and enabling more effective backjumping [ 6]. Model blocking is also explicit : whenever a complete model is found, a clause consisting of the negation of all its decision literals is added to exclude it in subsequent search. These blocking clauses persist across restarts, enabling the solver to reset the trail while retaining information about previously explored or infeasible regions. This approach is more robust to decision order, since restarts allow the solver to dynam-ically reorganize the search. Even if early decisions move the search away from promising regions, accumulated blocking and weight-conflict clauses guide the search toward high-weight models over time. The trade-off is that propagation becomes increasingly expensive as clauses accumulate, and memory usage may quickly increase. 

# 5 Experimental Evaluation 

We empirically evaluate the proposed WME framework on both synthetic and real-world benchmarks. Two tasks are considered: top-k and threshold-based enumeration. All experiments were conducted on a high-performance computing cluster. Each run was bound to a single core of an Intel Xeon E5-2650 v2 @ 2.60GHz with 25 GB RAM. We implemented two CDCL-based WME variants atop the same CaDiCaL [5] codebase: 

WME-NCB: Weighted model enumeration with non-chronological backtracking ;

WME-CB: Weighted model enumeration with chronological backtracking .

# 5.1 Benchmarks 

Following the TabularAllSAT evaluation methodology [ 40 ], we generated 525 random 3-CNF formulae with clause-to-variable ratio 1.5, close to the AllSAT phase-transition region [ 4], with 25 to 45 variables (dataset rnd3sat-1.5 ). These instances feature a large number of satisfying assignments, making them suitable for evaluating threshold-based enumeration. We G. Spallitta and M.Y. Vardi 13                        

> (a) Execution time (in seconds). (b) N. of score improvements.
> Figure 1 Top-1 enumeration results of the two WME frameworks on synthetic and real benchmarks. Notice that in all scatter plots the axes are log-scaled.
> (a) Top-1: WME-NCB vs. DPO (b) Top-1: WME-NCB vs. MaxHS (c) Native top-kvs. iterative top-1
> Figure 2 Top-kenumeration experiments. Left & center: top-1 performance of WME-NCB
> compared with state-of-the-art solvers. Right: median runtime of native top-kenumeration in
> WME-NCB compared with a baseline obtained by repeating the top-1 procedure.

further include 100 SATLIB random 3-CNF instances with a ratio of 4.28 and 200 variables, known to be challenging for SAT solvers and to provide a stress test under weighted pruning (dataset uf200-860 ). Literal weights for both benchmarks are sampled from (0 , 1) .We also evaluate 1049 literal-weighted CNF instances from [ 31 ], derived from Bayesian networks in [ 35 ]. Typically used for weighted model counting and MPE inference, these instances naturally induce a ranking of solutions by weight, aligning directly with WME. 

# 5.2 Top-k Enumeration 

Top-1 Enumeration Results 

We begin with the top-1 task, focusing on returning the highest-weight model within strict time limits (60 seconds). Since the results on the rnd3sat-1.5 instances are nearly identical for the two frameworks, we omit this benchmark suite. Figure 1 compares WME-NCB and WME-CB in terms of runtime (Figure 1a) and number of strict score improvements found during search (Figure 1b) on the remaining benchmarks. Across these benchmarks, WME-NCB consistently outperforms WME-CB in runtime. This is due to the rapid tightening of the bound θ after high-weight models are found: as θ increases, the framework triggers earlier weight conflicts and prunes large parts of the search space. 14 WME: Extending CDCL-based Model Enumeration with Weights                                                          

> Solver bayes-or (of 660) bayes-basic (of 389) uf200-860 (of 100) Fastest Solved PAR-2 Fastest Solved PAR-2 Fastest Solved PAR-2
> DPO 0660 0.240 0317 24.861 0060.000
> MaxHS 44 660 0.006 50 388 0.512 80 100 3.537
> WME-CB 55 660 0.027 0296 30.179 1100 5.022
> WME-NCB 561 660 0.007 339 383 1.887 19 100 2.235
> Table 3 Top-1 enumeration statistics across three benchmark families.
> Solver Solved (out of 525) PAR2 (s)
> WME-NCB 356 457.5
> WME-CB 450 267.5
> Table 4 Timeout statistics for thresholded WME framework variants on rnd3sat-1.5 .
> Figure 3 Threshold-based enumeration experimental evaluation among the two WME frameworks.

Restarts further exploit these clauses to steer the search toward higher score models. Despite unit propagation overhead, the reduction in explored branches leads to a net speedup. Figures 2a and 2b and Table 3 compare our faster framework WME-NCB with state-of-the-art baselines DPO [ 31 ] and MaxHS [10 ]. As expected, MaxHS , being a specialized MaxSAT engine, is highly competitive on top-1 extraction. Nonetheless, WME-NCB has competitive top-1 performance across the benchmark sets and substantially outperforms WME-CB and DPO ,particularly on the bayes-basic family. 

Native WME Top-k vs. Repeated WME Top-1 Extraction 

In addition, we evaluate the overhead of retrieving multiple high-weight solutions using native top-k enumeration, compared to repeatedly invoking the top-1 procedure with model blocking within the same framework. Figure 2c reports the median runtime of WME-NCB on the uf200-860 benchmarks for k ∈ { 1, 5, 10 }, together with the cost of the iterative baseline. The results suggest that iterative top-1 with blocking repeats a substantial amount of search effort across iterations, whereas native top-k reuses accumulated search information, including bounds and learned clauses, to extract successive high-weight solutions more efficiently. 

# 5.3 Threshold-Based Enumeration 

We next consider threshold-based enumeration. In this setting, the Bayesian network instances and the uf200-860 benchmarks contain only a few satisfying assignments, rendering the task largely trivial. We therefore focus on the rnd3sat-1.5 benchmarks, which feature a larger solution space and are standard for evaluating enumeration performance [ 4, 40 ]. For a formula with n variables, we set the threshold θ to 0.5n, retaining a non-trivial fraction of solutions above it. Since this setting typically requires enumerating many models, we use a timeout of 600 seconds. G. Spallitta and M.Y. Vardi 15       

> Figure 4 Ablation studies. Left: threshold-based enumeration. Right: top-1enumeration.

Figure 3 reports the runtime comparison. Unlike in the top-k case, WME-CB outperforms 

WME-NCB , especially on larger instances with many feasible models. This is expected, as non-chronological search accumulates blocking clauses that increasingly hinder propagation. These results are consistent with prior observations in unweighted enumeration [41, 40]. 

# 5.4 Ablation Studies 

Since WME integrates weight reasoning directly into the CDCL loop, it is natural to ask whether this integration is essential, or whether similar performance can be obtained by a pure AllSAT-style traversal followed by post hoc weight filtering. To this end, we compare our best-performing configuration with variants in which weight-based pruning is disabled. For top-1 enumeration, we evaluate WME-NCB on the challenging bayes-basic benchmarks; for threshold-based enumeration, we consider WME-CB on the rnd3sat-1.5 instances. In both cases (Figure 4), enabling weight-based conflict analysis achieves a substantial speedup, confirming that early weight-driven pruning is essential under weight constraints. 

# 6 Conclusion 

We introduced WME, a CDCL-based framework for enumerating models of a formula F

under weight-based criteria. The framework combines weight-conflict analysis for early pruning with residual-aware backtracking, allowing the search to resume only from states that may still lead to improved solutions. Our empirical results show that the choice between chronological and non-chronological backtracking is regime-dependent. In top-k settings, where the bound typically tightens quickly, non-chronological backtracking with blocking clauses often reaches high-weight solutions faster. In threshold-based enumeration, where many feasible models may need to be generated, chronological backtracking avoids the additional propagation burden of blocking clauses and is often preferable. Overall, the most effective design depends on the interaction between bound tightening, feasible-region density, and propagation overhead. One natural step is to make the solver adaptive, selecting between chronological and non-chronological reasoning based on structural properties of the instance and of the associated weight distribution. A second direction is to extend WME with projection and SMT reasoning, enabling weighted enumeration over variables of interest and under richer background theories. More broadly, these ideas may also be applied in future CDCL-based Weighted Model Integration algorithms to reduce the enumeration time [37, 38]. 16 WME: Extending CDCL-based Model Enumeration with Weights 

References 

1 A. Amarilli, P. Bourhis, F. Capelli, and M. Monet. Ranked Enumeration for MSO on Trees via Knowledge Compilation. In Graham Cormode and Michael Shekelyan, editors, 27th International Conference on Database Theory, ICDT 2024, Paestum, Italy, March 25-28, 2024 ,volume 290 of LIPIcs , pages 25:1–25:18. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2024. doi:10.4230/LIPICS.ICDT.2024.25 .

2 F. Bacchus, M. Järvisalo, J. Berg, R. Martins, and A. Niskanen. Maxsat evaluation 2020, 2020. 

3 C. Barrett, R. Sebastiani, A. A. Seshia, and C. Tinelli. Satisfiability Modulo Theories. In 

Handbook of Satisfiability , volume 336 of FAIA , pages 1267–1329. IOS Press, 2 edition, 2021. 

doi:10.3233/FAIA201017 .

4 R. J Bayardo Jr and R. Schrag. Using CSP look-back techniques to solve real-world SAT instances. In Aaai/iaai , pages 203–208. Providence, RI, 1997. 

5 A. Biere, T. Faller, K. Fazekas, M. Fleury, N. Froleyks, and F. Pollitt. CaDiCaL 2.0. In 

International Conference on Computer Aided Verification , pages 133–152. Springer, 2024. 

6 A. Biere, M. Heule, and H. van Maaren. Handbook of satisfiability , volume 185. IOS press, 2009. 

7 B. Cai, L. Huang, and M. Xie. Bayesian networks in fault diagnosis. IEEE Transactions on industrial informatics , 13(5):2227–2240, 2017. 

8 H. Chan and A. Darwiche. On the robustness of most probable explanations. In UAI ’06, Proceedings of the 22nd Conference in Uncertainty in Artificial Intelligence, Cambridge, MA, USA, July 13-16, 2006 . AUAI Press, 2006. 

9 A. Darwiche and P. Marquis. A knowledge compilation map. Journal of Artificial Intelligence Research , 17:229–264, 2002. 

10 J. Davies and F. Bacchus. Solving MAXSAT by solving a sequence of simpler SAT instances. In 

International conference on principles and practice of constraint programming , pages 225–239. Springer, 2011. 

11 P. Dilkas and V. Belle. Weighted model counting with conditional weights for bayesian networks. In Uncertainty in Artificial Intelligence , pages 386–396. PMLR, 2021. 

12 J. M. Dudek, V. H. N. Phan, and M. Y. Vardi. ADDMC: Weighted Model Counting with Algebraic Decision Diagrams. Proceedings of the AAAI Conference on Artificial Intelligence ,34(4):8730–8737, 2020. 

13 J. M. Dudek, V. HN Phan, and M. Y. Vardi. DPMC: weighted model counting by dynamic programming on project-join trees. In International Conference on Principles and Practice of Constraint Programming , pages 211–230. Springer, 2020. 

14 R. Fagin, A. Lotem, and M. Naor. Optimal aggregation algorithms for middleware. In 

Proceedings of the twentieth ACM SIGMOD-SIGACT-SIGART symposium on Principles of database systems , pages 102–113, 2001. 

15 M. Gebser, B. Kaufmann, A. Neumann, and T. Schaub. clasp: A conflict-driven answer set solver. In International Conference on Logic Programming and Nonmonotonic Reasoning ,pages 260–265. Springer, 2007. 

16 S. Joshi, P. Kumar, S. Rao, and R. Martins. Open-WBO-Inc: Approximation Strategies for Incomplete Weighted MaxSAT. Journal on Satisfiability, Boolean Modeling and Computation ,11(1):73–97, 2019. 

17 T. Korhonen and M. Järvisalo. SharpSAT-TD in model counting competitions 2021-2023. 

arXiv preprint arXiv:2308.15819 , 2023. 

18 J. Lagniez and P. Marquis. An improved decision-dnnf compiler. In IJCAI , volume 17, pages 667–673, 2017. 

19 E. L Lawler. A procedure for computing the k best solutions to discrete optimization problems and its application to the shortest path problem. Management science , 18(7):401–405, 1972. G. Spallitta and M.Y. Vardi 17 

20 W. Li, P. Poupart, and P. van Beek. Exploiting structure in weighted model counting approaches to probabilistic inference. Journal of Artificial Intelligence Research , 40:729–765, 2011. 

21 J. Liang, F. Ma, J. Zhou, and M. Yin. AllSATCC: Boosting AllSAT Solving with Efficient Component Analysis. In IJCAI , pages 1866–1872, 2022. 

22 Q. Liu and A. Ihler. Belief propagation for structured decision making. In Proceedings of the Twenty-Eighth Conference on Uncertainty in Artificial Intelligence , UAI’12, page 523–532, Arlington, Virginia, USA, 2012. AUAI Press. 

23 J. P. Marques-Silva and K. A. Sakallah. GRASP: A Search Algorithm for Propositional Satisfiability. IEEE Trans. Comput. , 48(5):506–521, May 1999. doi:10.1109/12.769433 .

24 R. Martins, V. Manquinho, and I. Lynce. Open-WBO: A Modular MaxSAT Solver. In Carsten Sinz and Ulrich Egly, editors, Theory and Applications of Satisfiability Testing – SAT 2014 ,pages 438–445, 2014. 

25 M. Michelutti, G. Masina, G. Spallitta, and R. Sebastiani. Canonical Decision Diagrams Modulo Theories. In Proceedings of the European Conference on Artificial Intelligence -ECAI24 , 2024. 

26 S. Minato, J. Kawahara, M. Banbara, T. Horiyama, I. Takigawa, and Y. Yamaguchi. Fast enumeration of all cost-bounded solutions for combinatorial problems using zdds. Discrete Applied Mathematics , 360:467–486, 2025. 

27 S. Möhle and A. Biere. Combining Conflict-Driven Clause Learning and Chronological Backtracking for Propositional Model Counting. In GCAI , pages 113–126, 2019. 

28 A. Nadel. TT - Open - WBO -Inc: An Efficient Anytime MaxSAT Solver. Journal on Satisfiability, Boolean Modeling and Computation , 15:1–7, 2024. 

29 R. Nieuwenhuis, A. Oliveras, and C. Tinelli. Solving SAT and SAT modulo theories: From an abstract Davis–Putnam–Logemann–Loveland procedure to DPLL (T). Journal of the ACM (JACM) , 53(6):937–977, 2006. 

30 U. Oztok and A. Darwiche. A top-down compiler for sentential decision diagrams. In IJCAI ,volume 15, pages 3141–3148, 2015. 

31 V. H. N. Phan and M. Y. Vardi. DPO: Dynamic-Programming Optimization on Hybrid Constraints. arXiv preprint arXiv:2205.08632 , 2022. 

32 D. A Plaisted and S. Greenbaum. A structure-preserving clause form translation. Journal of Symbolic Computation , 2(3):293–304, 1986. 

33 C. Rudin, C. Chen, Z. Chen, H. Huang, L. Semenova, and C. Zhong. Interpretable machine learning: Fundamental principles and 10 grand challenges. Statistic Surveys , 16:1–85, 2022. 

34 T. Sang, F. Bacchus, P. Beame, H. A. Kautz, and T. Pitassi. Combining component caching and clause learning for effective model counting. SAT , 4:7th, 2004. 

35 T. Sang, P. Beame, and H. A. Kautz. Performing Bayesian inference by weighted model counting. In AAAI , volume 5, pages 475–481, 2005. 

36 T. Sang, P. Beame, and H. A. Kautz. A Dynamic Approach for MPE and Weighted MAX-SAT. In IJCAI , pages 173–179, 2007. 

37 G. Spallitta, G. Masina, P. Morettin, A. Passerini, and R. Sebastiani. SMT-based weighted model integration with structure awareness. In Uncertainty in Artificial Intelligence , pages 1876–1885. PMLR, 2022. 

38 G. Spallitta, G. Masina, P. Morettin, A. Passerini, and R. Sebastiani. Enhancing SMT-based Weighted Model Integration by structure awareness. Artificial Intelligence , 328:104067, 2024. 

39 G. Spallitta, R. Sebastiani, and A. Biere. Disjoint partial enumeration without blocking clauses. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 38, pages 8126–8135, 2024. 

40 G. Spallitta, R. Sebastiani, and A. Biere. Disjoint projected enumeration for SAT and SMT without blocking clauses. Artificial Intelligence , page 104346, 2025. 

41 T. Toda and T. Soh. Implementing efficient all solutions SAT solvers. Journal of Experimental Algorithmics (JEA) , 21:1–44, 2016. 18 WME: Extending CDCL-based Model Enumeration with Weights 

42 D. Tolpin and F. Wood. Maximum a posteriori estimation by search in probabilistic programs. In Proceedings of the International Symposium on Combinatorial Search , volume 6, pages 201–205, 2015. 

43 G. S. Tseitin. On the complexity of derivation in propositional calculus. In Automation of Reasoning 2: Classical Papers on Computational Logic 1967-70 , pages 466–483. Springer, 1983. 

44 C. Yanover and Y. Weiss. Finding the M most probable configurations using loopy belief propagation. Advances in neural information processing systems , 16, 2003.