---
title: "Reinforced Generation of Combinatorial Structures: Ramsey Numbers"
title_zh: 组合结构的强化生成：拉姆齐数
authors: "Ansh Nagda, Prabhakar Raghavan, Abhradeep Thakurta"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09172v2"
tags: ["query:ml-co"]
score: 9.0
evidence: 使用基于大模型的智能体强化生成拉姆齐数等组合结构
tldr: "本研究利用基于大语言模型的代码演化智能体 AlphaEvolve，在组合数学领域取得了突破。通过该通用元算法，研究者成功提高了五个经典拉姆齐数（Ramsey numbers）的下界，包括 R(3, 13) 到 R(4, 15) 等。AlphaEvolve 不仅能复现所有已知的精确下界，还能在无需针对特定问题定制算法的情况下，匹配多种复杂情况下的最佳已知结果，展示了 LLM 在自动化搜索算法设计中的巨大潜力。"
selection_source: fresh_fetch
motivation: 旨在解决传统拉姆齐数搜索高度依赖针对特定问题手工设计的定制化算法、缺乏通用性的局限性。
method: 提出了一种名为 AlphaEvolve 的 LLM 驱动的代码变异智能体，作为能够自动生成高效搜索算法的通用元算法。
result: 成功提高了五个经典拉姆齐数的下界，并复现了所有已知的精确下界，同时在多个案例中匹配了现有的最佳结果。
conclusion: 证明了基于 LLM 的自动化代码演化方法在解决复杂组合数学问题和优化搜索算法方面具有极高的有效性和通用性。
---

## 摘要
我们提出了五个经典拉姆齐数（Ramsey numbers）的改进下界：$\mathbf{R}(3, 13)$ 从 $60$ 提高到 $61$，$\mathbf{R}(3, 18)$ 从 $99$ 提高到 $100$，$\mathbf{R}(4, 13)$ 从 $138$ 提高到 $139$，$\mathbf{R}(4, 14)$ 从 $147$ 提高到 $148$，以及 $\mathbf{R}(4, 15)$ 从 $158$ 提高到 $159$。这些结果是使用 AlphaEvolve 实现的，它是一个基于大语言模型（LLM）的代码变异智能体。除了这些新结果外，我们还成功复现了所有已知精确值的拉姆齐数的下界，并在许多其他情况下达到了已知的最佳下界。其中包括一些先前工作中未详细说明所用算法的下界。几乎所有已知的拉姆齐数下界都是通过计算得出的，通常使用定制的搜索算法，每种算法仅能提供少数结果。AlphaEvolve 是一种单一的元算法，它为我们所有的结果生成了相应的搜索算法。

## Abstract
We present improved lower bounds for five classical Ramsey numbers: $\mathbf{R}(3, 13)$ is increased from $60$ to $61$, $\mathbf{R}(3, 18)$ from $99$ to $100$, $\mathbf{R}(4, 13)$ from $138$ to $139$, $\mathbf{R}(4, 14)$ from $147$ to $148$, and $\mathbf{R}(4, 15)$ from $158$ to $159$. These results were achieved using AlphaEvolve, an LLM-based code mutation agent. Beyond these new results, we successfully recovered lower bounds for all Ramsey numbers known to be exact, and matched the best known lower bounds across many other cases. These include bounds for which previous work does not detail the algorithms used. Virtually all known Ramsey lower bounds are derived computationally, with bespoke search algorithms each delivering a handful of results. AlphaEvolve is a single meta-algorithm yielding search algorithms for all of our results.

---

## 论文详细总结（自动生成）

这篇论文展示了人工智能在组合数学前沿领域的突破性应用，以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：拉姆齐数 $R(r, s)$ 是组合数学中的经典难题，定义为确保一个 $n$ 个顶点的图中必然包含大小为 $r$ 的团（clique）或大小为 $s$ 的独立集（independent set）的最小整数 $n$。
*   **核心问题**：寻找拉姆齐数的下界通常需要构造出不包含特定团和独立集的极大图。
*   **研究动机**：传统上，这些下界是由人类专家针对特定参数 $(r, s)$ 手工设计的定制化搜索算法（如模拟退火、禁忌搜索等）发现的。本研究旨在探索是否能用一种通用的“元算法”自动发现这些高效的搜索策略，从而突破人类设计的局限。

### 2. 论文提出的方法论
论文采用了 **AlphaEvolve**，这是一个基于大语言模型（LLM）的代码变异智能体。
*   **核心思想**：不直接搜索图结构，而是搜索“能够生成图的算法代码”。通过 LLM 对代码进行迭代变异和演化，自动发现针对特定拉姆齐参数的最优搜索启发式。
*   **关键技术细节**：
    *   **演化循环**：维护一个算法种群 $P$，利用 LLM 对选定的父代算法进行变异生成新算法 $p_{new}$。
    *   **双重输出**：每个算法运行后输出两个图：主图 $G_1$（验证下界）和更大的“前景图” $G_2$（用于引导搜索方向）。
    *   **评分机制**：
        *   如果 $G_1$ 合法，根据其顶点数 $v_1$ 评分，若超过当前最佳（SoTA）则给予高额加权奖励。
        *   对 $G_2$ 引入“违规奖励”：即使 $G_2$ 不合法，但如果其包含的团/独立集数量低于随机图的期望值 $E_{viol}$，也会获得额外分数，从而引导算法向“几乎合法”的大图演化。
*   **算法分类**：AlphaEvolve 自动生成的算法涵盖了四类初始化策略：随机/随机初始化、Paley/代数种子、循环/循环引导、以及混合/分形/谱初始化。

### 3. 实验设计
*   **实验场景**：针对 $r, s \le 22$ 的“小”经典拉姆齐数。
*   **Benchmark（基准）**：以 Radziszowski (2024) 维护的权威拉姆齐数综述作为对比标准。
*   **对比方法**：对比了过去几十年中由 Exoo、Tatarevic、Kolodyazhny 等专家提出的各种计算方法。
*   **验证方式**：所有新发现的图构造均已在 GitHub 公开，供社区验证。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算细节**：文中提到了“计算预算”（compute budget）的概念，并指出对于 Table 1 中的规模，团计数（clique counting）并非计算瓶颈，暗示实验在可接受的计算时间内完成。

### 5. 实验数量与充分性
*   **实验规模**：研究覆盖了 28 个拉姆齐数单元格。
*   **充分性**：
    *   **突破性实验**：成功提高了 5 个经典拉姆齐数的下界。
    *   **复现性实验**：成功复现了所有已知精确值的拉姆齐数下界，并匹配了多个 SoTA 结果。
    *   **多样性**：展示了针对不同参数生成的 20 多个具体算法（见附录），证明了元算法在不同约束下的适应性。
*   **客观性**：实验对比基于公认的学术综述，结果具有高度的客观性和公信力。

### 6. 论文的主要结论与发现
*   **新下界发现**：
    *   $R(3, 13) \ge 61$（原 60）
    *   $R(3, 18) \ge 100$（原 99）
    *   $R(4, 13) \ge 139$（原 138）
    *   $R(4, 14) \ge 148$（原 147）
    *   $R(4, 15) \ge 159$（原 158）
*   **算法演化能力**：AlphaEvolve 能够自动发现人类专家曾使用的复杂代数结构（如 Paley 图、三次剩余图），甚至能组合出人类未曾报道过的新型启发式策略（如“分形镜像初始化”或“谐波隧道效应”）。
*   **通用性**：单一的元算法框架可以替代多种针对特定问题的手工算法。

### 7. 优点
*   **自动化程度极高**：将人类从繁琐的启发式算法调优中解放出来，实现了“算法发现算法”。
*   **超越人类直觉**：生成的算法往往结合了多种数学技巧（如代数种子 + 禁忌搜索 + 动态权重），展现了比单一手工算法更强的搜索效能。
*   **引导机制创新**：通过奖励“低违规”的非法图，成功解决了极值搜索中常见的“奖励稀疏”问题。

### 8. 不足与局限
*   **计算成本未知**：虽然算法高效，但演化过程本身可能消耗大量 LLM 推理资源。
*   **算法特异性**：AlphaEvolve 为每个单元格生成的算法往往是高度特异的，难以直接迁移到其他参数（例如为 $R(3, 13)$ 生成的算法在 $R(3, 14)$ 上表现不佳）。
*   **仅限于下界**：该方法本质上是构造性的，只能寻找反例来提高下界，无法解决证明非存在性的上界问题。
*   **黑盒属性**：虽然生成的代码是可读的，但演化出特定策略的深层逻辑（为什么某些代数结构在特定参数下有效）仍缺乏严谨的理论解释。

（完）
