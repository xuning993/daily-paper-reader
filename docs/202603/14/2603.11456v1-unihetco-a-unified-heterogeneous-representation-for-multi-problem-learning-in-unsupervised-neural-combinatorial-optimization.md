---
title: "UniHetCO: A Unified Heterogeneous Representation for Multi-Problem Learning in Unsupervised Neural Combinatorial Optimization"
title_zh: UniHetCO：无监督神经组合优化中多问题学习的统一异构表示
authors: "Kien X. Nguyen, Ilya Safro"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11456v1"
tags: ["query:ml-co"]
score: 10.0
evidence: 用于无监督神经组合优化的统一异构图表示
tldr: 针对无监督神经组合优化中现有方法难以跨问题类学习的局限，本文提出UniHetCO框架。该框架通过统一的异构图表示，将问题结构、目标项和线性约束编码为单一输入，并采用统一的无标签目标函数进行训练。结合梯度范数动态加权方案，UniHetCO实现了在多个约束组合优化问题上的联合学习，不仅在性能上媲美现有基准，还展现出强大的跨问题适应能力和为传统求解器提供高质量热启动的能力。
selection_source: fresh_fetch
motivation: 现有的无监督神经组合优化方法通常局限于单一问题类且依赖特定损失函数，难以在统一框架下实现跨问题的联合学习。
method: 提出一种统一的异构图表示方法，将约束二次规划问题的结构、目标和线性约束编码，并引入梯度范数动态加权以平衡多任务学习。
result: 实验证明该方法在四类约束问题上达到了最先进的性能，并展现出显著的跨问题适应潜力和对商业求解器的热启动加速效果。
conclusion: UniHetCO通过统一的表示与目标函数，成功实现了多问题无监督神经组合优化的通用化处理，提升了模型的泛化与实用性。
---

## 摘要
无监督神经组合优化（NCO）通过在没有真值解的情况下训练基于学习的求解器，直接最小化实例目标和约束违反，为监督方法提供了一种极具吸引力的替代方案。然而，对于图节点子集选择问题（如最大团和最大独立集），现有的无监督方法通常专门针对单一问题类别，并依赖于特定问题的代理损失，这阻碍了在统一框架内进行跨类别的学习。在这项工作中，我们提出了 UniHetCO，这是一种用于基于约束二次规划的组合优化的统一异构图表示，它将问题结构、目标项和线性约束编码在单个输入中。这种建模方式使得能够使用统一的无标签目标在多个问题类别上训练单个模型。为了提高多问题学习下的稳定性，我们采用了一种基于梯度范数的动态加权方案，以缓解类别间的梯度不平衡。在多个数据集和四个受限问题类别上的实验表明，该方法与最先进的无监督 NCO 基准相比具有竞争力的性能，展现了强大的跨问题适应潜力，并在严格的时间限制下为商业经典求解器提供了有效的热启动。

## Abstract
Unsupervised neural combinatorial optimization (NCO) offers an appealing alternative to supervised approaches by training learning-based solvers without ground-truth solutions, directly minimizing instance objectives and constraint violations. Yet for graph node subset-selection problems (e.g., Maximum Clique and Maximum Independent Set), existing unsupervised methods are typically specialized to a single problem class and rely on problem-specific surrogate losses, which hinders learning across classes within a unified framework. In this work, we propose UniHetCO, a unified heterogeneous graph representation for constrained quadratic programming-based combinatorial optimization that encodes problem structure, objective terms, and linear constraints in a single input. This formulation enables training a single model across multiple problem classes with a unified label-free objective. To improve stability under multi-problem learning, we employ a gradient-norm-based dynamic weighting scheme that alleviates gradient imbalance among classes. Experiments on multiple datasets and four constrained problem classes demonstrate competitive performance with state-of-the-art unsupervised NCO baselines, strong cross-problem adaptation potential, and effective warm starts for a commercial classical solver under tight time limits.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniHetCO: A Unified Heterogeneous Representation for Multi-Problem Learning in Unsupervised Neural Combinatorial Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的无监督神经组合优化（NCO）方法通常针对单一问题（如最大独立集）设计特定的代理损失函数和模型架构。这种“一题一模”的范式存在两个主要缺陷：
*   **缺乏通用性**：无法在统一框架下处理具有不同目标和约束的多种组合优化问题。
*   **学习效率低**：难以在结构相关的不同问题之间实现知识迁移或联合训练。
**研究动机**：提出一种能够将多种约束组合优化问题统一表示并进行联合无监督训练的通用模型，实现“一个模型解决多类问题”。

### 2. 方法论：核心思想与技术细节
UniHetCO 的核心在于将组合优化问题转化为**二次规划（QP）**形式，并通过**异构图**进行统一编码。

*   **统一异构表示**：
    *   **节点类型**：包含“变量节点”（决策变量 $x_i$）和“约束节点”（代表线性约束 $Ax \le b$）。
    *   **边类型**：
        1.  **原始图边**：捕捉实例本身的拓扑结构。
        2.  **目标函数边**：编码二次项系数矩阵 $Q$ 和线性项 $c$（转化为 QUBO 形式）。
        3.  **约束边**：连接变量节点与约束节点，权重为约束矩阵 $A$ 的系数。
*   **统一无监督损失**：利用 QUBO 形式构建通用的代理损失函数，包含目标项最小化和约束违反惩罚项（Penalty），使得模型无需真值标签即可训练。
*   **动态加权方案（GradNorm）**：在多问题联合训练时，不同问题的损失尺度差异巨大。UniHetCO 引入了基于梯度范数的动态加权策略，通过实时调整各问题的损失权重，防止梯度较大的问题主导参数更新，确保训练稳定性。
*   **模型架构**：采用异构图神经网络（Heterogeneous GNN），针对不同关系类型使用不同的消息传递通道，最后融合特征输出每个节点的选择概率。

### 3. 实验设计
*   **数据集**：
    *   社交网络数据：IMDB, COLLAB, Twitter。
    *   合成基准：RB200。
    *   结构多样性图：SparseSuit Matrix Collection（包含电力网络、材料科学等领域的真实图）。
*   **问题类别**：最大独立集 (MIS)、最大团 (MC)、最小顶点覆盖 (MVC)、最小支配集 (MDS)。
*   **Benchmark 与对比方法**：
    *   **无监督 SOTA**：EGN, Meta-EGN。
    *   **传统算法**：贪心算法（Greedy）。
    *   **精确求解器**：Gurobi v12.03（作为 Oracle 提供最优解，或在严格时限下作为对比基准）。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **80GB 显存的 NVIDIA A100 GPU** 上完成。
*   **训练配置**：
    *   单问题设置：Batch size 64，训练 50 epochs。
    *   多问题设置：每个问题类 Batch size 32，训练 100 epochs。
    *   优化器：使用 RMSprop，学习率为 0.001。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **单问题对比**：在 4 个数据集上对比了 MC 和 MVC 问题，证明了统一表示在单任务下也不逊于专用模型。
*   **多问题联合训练**：在 Twitter、RB200 和 SparseSuit 上验证了 UniHetCO-DW（动态加权版）的稳健性。
*   **跨问题泛化（Zero-shot）**：进行了“留一法”实验，测试模型在未见过的问题类上的迁移能力。
*   **热启动实验**：测试了模型输出作为 Gurobi 初始解对求解速度的提升。
*   **消融实验**：对比了经验风险最小化（ERM）、静态加权（SW）与动态加权（DW）的效果。

### 6. 主要结论与发现
*   **性能对标**：UniHetCO 在单问题上达到了与 Meta-EGN 等最先进方法相当的水平，在某些挑战性数据集（如 RB200）上甚至有所超越。
*   **联合学习可行性**：通过异构图编码，单个模型可以同时学习四种不同的组合优化问题，虽然性能较单任务模型略有下降，但保持了极高的近似比。
*   **动态加权至关重要**：实验证明，如果没有梯度平衡，多问题训练极易失败或偏向某一类问题。
*   **跨问题迁移**：模型在 MC 和 MDS 问题上展现了较强的零样本迁移和快速微调能力，但在 MIS 和 MVC 上的跨类迁移效果有限。
*   **加速传统求解器**：UniHetCO 的预测结果能显著提升 Gurobi 在极短时间限制（0.2s）内的求解质量。

### 7. 优点（亮点）
*   **通用性突破**：首次在无监督 NCO 领域实现了通过异构图统一表示目标函数和线性约束，打破了“一题一损”的限制。
*   **硬件友好性**：基于 QUBO 的建模方式与量子处理器或 CMOS 加速器等新兴硬件的接口高度契合。
*   **端到端处理**：将约束直接编码进输入图，而非仅仅在损失函数中体现，增强了模型对约束结构的感知。

### 8. 不足与局限
*   **计算开销**：对于非局部约束（如全局约束），将其显式编码为约束节点会导致异构图规模迅速膨胀，增加消息传递的计算负担。
*   **跨类迁移不平衡**：实验显示某些问题类之间的知识迁移并不对称，模型在处理高度异构的任务分布时仍存在稳定性挑战。
*   **近似比损失**：在多问题联合训练模式下，部分任务的性能相比专门针对该任务优化的单模型仍有小幅差距。

（完）
