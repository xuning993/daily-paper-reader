---
title: Cluster-Aware Attention-Based Deep Reinforcement Learning for Pickup and Delivery Problems
title_zh: 针对取送货问题的聚类感知注意力深度强化学习
authors: "Wentao Wang, Lifeng Han, Guangyu Zou"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.10053v1"
tags: ["query:ml-co"]
score: 10.0
evidence: 深度强化学习解决取送货问题
tldr: 针对取送货问题（PDP）中节点成对约束和空间聚类特性，本文提出CAADRL框架。该框架通过聚类感知编码器结合全局与簇内注意力，并利用动态双解码器平衡簇内路径与簇间转移。实验表明，CAADRL在处理聚类分布问题时表现优异，且推理速度显著快于现有的神经协作搜索方法，为解决复杂PDP提供了高效的归纳偏置。
selection_source: fresh_fetch
motivation: 现有的深度强化学习方法在处理具有空间聚类特性的PDP问题时，往往难以兼顾约束处理效率与推理速度。
method: 提出一种结合聚类感知编码与动态双解码器的层次化强化学习框架，通过学习门控平衡簇内路由与簇间转移。
result: 在聚类和均匀分布的PDP基准测试中，该方法在保持高性能的同时，推理速度显著优于现有的神经协作搜索基准。
conclusion: 显式建模聚类结构为神经PDP求解器提供了有效的归纳偏置，证明了层次化处理在解决复杂空间布局路径规划中的优越性。
---

## 摘要
取送货问题 (PDP) 是车辆路径问题的一种基础且具有挑战性的变体，其特点是紧密耦合的取送货对、优先级约束以及通常表现出聚类特征的空间布局。现有的深度强化学习 (DRL) 方法要么在扁平图上对所有节点建模，依靠隐式学习来强制执行约束；要么通过推理时的协作搜索获得强大性能，但代价是产生巨大的延迟。在本文中，我们提出了 CAADRL（聚类感知注意力深度强化学习），这是一个通过聚类感知编码和分层解码显式利用 PDP 实例多尺度结构的 DRL 框架。编码器基于 Transformer，结合了全局自注意力以及针对仓库、取货和送货节点的簇内注意力，生成的嵌入既具有全局信息量，又具有局部角色感知能力。基于这些嵌入，我们引入了一个带有可学习门控的动态双解码器，在每一步平衡簇内路由和簇间转换。该策略采用 POMO 风格的策略梯度方案进行端到端训练，每个实例使用多个对称展开。在合成的聚类和均匀 PDP 基准测试上的实验表明，CAADRL 在聚类实例上达到或优于强大的最先进基准，并且在均匀实例上保持高度竞争力，特别是在问题规模增加时。至关重要的是，我们的方法在推理时间远低于神经协作搜索基准的情况下实现了这些结果，这表明显式建模聚类结构为神经 PDP 求解器提供了一种有效且高效的归纳偏置。

## Abstract
The Pickup and Delivery Problem (PDP) is a fundamental and challenging variant of the Vehicle Routing Problem, characterized by tightly coupled pickup--delivery pairs, precedence constraints, and spatial layouts that often exhibit clustering. Existing deep reinforcement learning (DRL) approaches either model all nodes on a flat graph, relying on implicit learning to enforce constraints, or achieve strong performance through inference-time collaborative search at the cost of substantial latency. In this paper, we propose \emph{CAADRL} (Cluster-Aware Attention-based Deep Reinforcement Learning), a DRL framework that explicitly exploits the multi-scale structure of PDP instances via cluster-aware encoding and hierarchical decoding. The encoder builds on a Transformer and combines global self-attention with intra-cluster attention over depot, pickup, and delivery nodes, producing embeddings that are both globally informative and locally role-aware. Based on these embeddings, we introduce a Dynamic Dual-Decoder with a learnable gate that balances intra-cluster routing and inter-cluster transitions at each step. The policy is trained end-to-end with a POMO-style policy gradient scheme using multiple symmetric rollouts per instance. Experiments on synthetic clustered and uniform PDP benchmarks show that CAADRL matches or improves upon strong state-of-the-art baselines on clustered instances and remains highly competitive on uniform instances, particularly as problem size increases. Crucially, our method achieves these results with substantially lower inference time than neural collaborative-search baselines, suggesting that explicitly modeling cluster structure provides an effective and efficient inductive bias for neural PDP solvers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CAADRL**（聚类感知注意力深度强化学习）的新型框架，旨在高效解决取送货问题（Pickup and Delivery Problem, PDP）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：PDP 是车辆路径问题（VRP）的一个复杂变体，要求同一辆车完成取货和送货，且必须满足“先取后送”的优先级约束。
*   **研究动机**：
    *   **空间聚类特性**：现实中的取送货点往往呈区域性分布（如工业区取货、居民区送货），但现有 DRL 方法多将节点视为扁平图，忽略了这种多尺度空间结构。
    *   **效率与性能的权衡**：现有的神经求解器要么依赖复杂的推理时搜索（如 NCS）导致高延迟，要么仅靠隐式学习约束导致解质量有限。
*   **整体含义**：通过在模型架构中引入显式的“聚类感知”归纳偏置，提升模型在处理具有空间结构特征的 PDP 问题时的效率和泛化能力。

### 2. 方法论：核心思想与关键技术
CAADRL 采用编码器-解码器架构，核心创新点包括：
*   **聚类感知编码器 (Cluster-Aware Encoder)**：
    *   在标准 Transformer 基础上，每层包含两个并行的注意力分支：**全局自注意力**（捕捉全局空间依赖）和**簇内注意力**（利用掩码限制节点仅关注同类簇节点，如仅看取货点）。
    *   通过融合这两者，生成的嵌入向量既包含全局上下文，又具备局部角色感知能力。
*   **分层动态双解码器 (Dynamic Dual-Decoder)**：
    *   设计了两个并行的解码管道：一个负责**簇内路由**（局部战术决策），另一个负责**簇间转移**（全局战略决策）。
    *   引入**可学习门控机制 (Gating Mechanism)**：根据当前状态动态输出一个概率 $p_{stay}$，用于加权融合两个解码器的输出，决定是继续在当前区域服务还是跳转到新区域。
*   **训练算法**：采用 **POMO (Policy Optimization with Multiple Optima)** 风格的强化学习训练，通过从不同起始节点展开多个对称路径来降低梯度方差，提高训练稳定性。

### 3. 实验设计
*   **数据集/场景**：
    *   **聚类分布 (Clustered)**：取货点和送货点分别服从不同的正态分布，模拟明显的区域分离。
    *   **均匀分布 (Uniform)**：所有节点在平面内随机均匀分布，测试模型的鲁棒性。
*   **问题规模**：PDP10、PDP20、PDP40、PDP80（数字代表总节点数）。
*   **Benchmark 与对比方法**：
    *   **NCS (Neural Collaborative Search)**：目前最强的基于搜索的神经求解器。
    *   **Heter-AM**：采用异构注意力的神经构造方法。
    *   **解码策略对比**：包括贪婪解码 (Greedy) 和不同规模的采样解码 (Sampling-1280/12800)。

### 4. 资源与算力
*   **硬件**：文中提到测试是在 **vGPU-32GB** 显卡上独立进行的。
*   **训练参数**：
    *   训练 800 个 epoch，Batch Size 为 512。
    *   使用 Adam 优化器，学习率固定为 $1 \times 10^{-4}$。
    *   模型配置：128 维嵌入，6 层编码器，8 头注意力。
*   **时长**：未明确给出总训练时长，但强调了推理时间（PDP80 规模下推理时间小于 0.2 秒）。

### 5. 实验数量与充分性
*   **实验覆盖面**：
    *   涵盖了两种截然不同的数据分布（聚类 vs 均匀）。
    *   进行了**消融实验**，分别验证了聚类编码器、双解码器和 POMO 训练对性能的贡献。
    *   进行了**跨规模泛化实验**（如 PDP100 训练模型直接测试 PDP200/300/500）。
*   **充分性评价**：实验设计较为全面，不仅对比了路径长度（解质量），还详细对比了推理时间，证明了该方法在效率上的显著优势。跨规模泛化的测试增强了结论的说服力。

### 6. 主要结论与发现
*   **聚类场景领先**：在聚类分布下，CAADRL 在中大规模问题（PDP40/80）上优于 NCS 和 Heter-AM。
*   **推理效率极高**：作为纯构造式策略，CAADRL 的推理速度远快于需要多次迭代改进的 NCS 框架。
*   **泛化能力强**：即使在没有明显聚类特征的均匀分布下，CAADRL 依然保持了极强的竞争力，甚至在 PDP80 规模下超过了基准方法。
*   **规模可扩展性**：模型展现出良好的零样本（Zero-shot）泛化能力，能够处理比训练规模大数倍的问题。

### 7. 优点
*   **归纳偏置设计巧妙**：显式建模“簇内”与“簇间”决策，符合人类专家解决路径规划问题的直觉。
*   **端到端高效性**：无需复杂的后处理或迭代搜索，单次自回归解码即可获得高质量解。
*   **鲁棒性**：虽然针对聚类设计，但在非聚类数据上并未出现性能退化。

### 8. 不足与局限
*   **合成数据局限**：实验主要基于合成的 2D 欧几里得空间数据，未在真实路网（如基于 OpenStreetMap 的非对称距离）上验证。
*   **约束单一**：目前仅考虑了基础的优先级约束，未涉及时间窗（TW）、车辆容量（Capacitated）或多车协同等更复杂的现实约束。
*   **聚类定义预设**：模型依赖于预定义的聚类 ID（取货簇/送货簇），对于更复杂的动态聚类场景，可能需要更灵活的自动聚类机制。

（完）
