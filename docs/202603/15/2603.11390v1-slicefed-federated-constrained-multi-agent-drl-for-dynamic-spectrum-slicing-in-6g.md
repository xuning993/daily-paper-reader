---
title: "SliceFed: Federated Constrained Multi-Agent DRL for Dynamic Spectrum Slicing in 6G"
title_zh: SliceFed：面向 6G 动态频谱切片的联邦约束多智能体深度强化学习
authors: "Hossein Mohammadi, Seyed Bagher Hashemi Natanzi, Ramak Nassiri, Jamshid Hassanpour, Bo Tang, Vuk Marojevic"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11390v1"
tags: ["query:ml-co"]
score: 8.0
evidence: 多智能体深度强化学习用于频谱切片资源分配
tldr: 针对6G无线接入网中动态频谱切片的资源分配难题，本文提出SliceFed框架。该框架结合联邦学习与受限多智能体深度强化学习（F-MADRL），将切片问题建模为受限马尔可夫决策过程。通过拉格朗日原对偶PPO算法和联邦平均机制，在保护隐私的同时，实现了频谱效率最大化并严格满足URLLC时延约束，为高密度干扰环境下的6G频谱管理提供了可靠方案。
selection_source: fresh_fetch
motivation: 旨在解决6G高密度干扰环境下，如何在满足严格QoS要求和数据隐私的前提下，优化异构服务的动态频谱资源分配问题。
method: 提出SliceFed框架，采用受限多智能体强化学习（CMDP）结合拉格朗日原对偶PPO算法，并利用联邦学习实现多基站间的协作训练。
result: "仿真结果显示SliceFed实现了近100%的1ms URLLC时延达标率，且在不同流量负载下均表现出优于基准方案的鲁棒性。"
conclusion: SliceFed通过融合联邦学习与受限强化学习，为实现6G网络中安全、可靠且具备扩展性的动态频谱管理提供了有效途径。
---

## 摘要
动态频谱切片是 6G 无线接入网（RAN）的关键赋能技术，支持异构服务的共存。然而，由于非平稳信道动态、严格的服务质量（QoS）要求以及对数据隐私的需求，在密集、受干扰限制的部署中优化资源分配仍然具有挑战性。在本文中，我们提出了 SliceFed，一种新型的联邦约束多智能体深度强化学习（F-MADRL）框架。SliceFed 将切片问题建模为约束马尔可夫决策过程（CMDP），其中自主 gNB 智能体在最大化频谱效率的同时，明确满足小区内干扰预算和硬性超高可靠低延迟通信（URLLC）延迟期限。我们采用集成近端策略优化（PPO）的拉格朗日原对偶方法来实施约束，同时通过联邦平均（Federated Averaging）在不交换原始本地数据的情况下实现协作学习。在密集多小区环境中的广泛仿真表明，SliceFed 收敛于一种稳定且具有安全意识的策略。与启发式和无约束基准相比，SliceFed 实现了近 100% 的 1 ms URLLC 延迟期限满足率，并对流量负载变化表现出卓越的鲁棒性，验证了其在可靠且可扩展的 6G 频谱管理方面的潜力。

## Abstract
Dynamic spectrum slicing is a critical enabler for 6G Radio Access Networks (RANs), allowing the coexistence of heterogeneous services. However, optimizing resource allocation in dense, interference-limited deployments remains challenging due to non-stationary channel dynamics, strict Quality-of-Service (QoS) requirements, and the need for data privacy. In this paper, we propose SliceFed, a novel Federated Constrained Multi-Agent Deep Reinforcement Learning (F-MADRL) framework. SliceFed formulates the slicing problem as a Constrained Markov Decision Process (CMDP) where autonomous gNB agents maximize spectral efficiency while explicitly satisfying inter-cell interference budgets and hard ultra-reliable low-latency communication (URLLC) latency deadlines. We employ a Lagrangian primal-dual approach integrated with Proximal Policy Optimization (PPO) to enforce constraints, while Federated Averaging enables collaborative learning without exchanging raw local data. Extensive simulations in a dense multi-cell environment demonstrate that SliceFed converges to a stable, safety-aware policy. Unlike heuristic and unconstrained baselines, SliceFed achieves nearly 100% satisfaction of 1~ms URLLC latency deadlines and exhibits superior robustness to traffic load variations, verifying its potential for reliable and scalable 6G spectrum management.

---

## 论文详细总结（自动生成）

这是一份关于论文《SliceFed: Federated Constrained Multi-Agent DRL for Dynamic Spectrum Slicing in 6G》的结构化深入分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：在 6G 无线接入网（RAN）中，如何在高密度、强干扰的环境下，为异构服务（如 eMBB、URLLC、mMTC）实现高效的动态频谱切片。
*   **研究背景**：
    *   **动态性与复杂性**：无线信道具有非平稳特征，且流量需求波动剧烈，传统基于规则的方法难以应对。
    *   **严格的 QoS 约束**：尤其是 URLLC 服务对延迟（通常要求 < 1ms）和可靠性有极高要求。
    *   **隐私与协作矛盾**：多基站（gNB）之间需要协作以减少干扰，但出于隐私考虑，不能直接交换原始流量或信道数据。
    *   **现有方案不足**：传统的强化学习（RL）多采用奖励塑造（Reward Shaping）来处理约束，缺乏硬性的安全保证。

### 2. 方法论与核心技术
论文提出了 **SliceFed** 框架，其核心思想是将频谱切片建模为**约束马尔可夫决策过程（CMDP）**，并结合联邦学习进行分布式求解。
*   **核心模型 (CMDP)**：每个基站作为一个智能体，目标是在满足三个硬性约束（小区外干扰泄漏预算、URLLC 延迟期限、资源分配可行性）的前提下，最大化长期系统效用（频谱效率、QoS 满意度和重配置成本的加权和）。
*   **关键技术细节**：
    *   **拉格朗日原对偶 PPO 算法**：将受限优化问题转化为无约束的鞍点问题。通过引入对偶变量（乘子）来惩罚违反约束的行为，动态调整奖励函数。
    *   **联邦多智能体架构 (F-MADRL)**：采用联邦平均（FedAvg）机制。各基站本地训练 PPO 模型，仅向中央服务器上传模型参数更新（梯度/权重），而不共享本地原始数据。
    *   **连续动作空间处理**：将切片决策建模为连续向量（带宽比例），利用 PPO 算法的裁剪更新特性保证训练稳定性，避免资源分配的剧烈震荡。
    *   **策略内聚与探索**：引入策略蒸馏损失项，减少本地策略与全局共识策略的偏差，并使用熵正则化鼓励探索。

### 3. 实验设计与基准对比
*   **实验场景**：模拟了一个包含 7 个六边形小区的密集 RAN 环境，每个小区服务 10 个用户，系统带宽 20MHz，调度间隔 1ms。
*   **流量模型**：eMBB、URLLC 和 mMTC 流量遵循独立的泊松分布，其中 URLLC 设有 1ms 的硬性延迟期限。
*   **对比方法 (Benchmarks)**：
    *   **Equal Slicing (等分切片)**：资源在各切片间静态均分。
    *   **Queue-Proportional (QueueProp)**：根据各切片瞬时队列长度按比例分配带宽（启发式）。
    *   **Random Allocation (随机分配)**：基于狄利克雷分布随机采样资源比例。
*   **评估指标**：收敛奖励值、约束违反量（g1, g2）、URLLC 延迟累积分布函数（CDF）、队列长度动态、流量负载鲁棒性。

### 4. 资源与算力消耗
*   **软件环境**：基于 PyTorch 实现。
*   **网络结构**：Actor 和 Critic 网络均包含两个 128 神经元的隐藏层，使用 ReLU 激活函数。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了训练参数：200 个全局联邦轮次，每个本地更新周期包含 1000 个时间步的轨迹采样。

### 5. 实验数量与充分性评价
*   **实验规模**：进行了收敛性分析、可靠性分析、时域动态分析以及针对不同 URLLC 负载（2 到 6 packets/slot）的鲁棒性扫描。
*   **充分性与客观性**：
    *   实验结果基于 **5 个随机种子**的平均值，具有一定的统计说服力。
    *   对比了静态、启发式和随机三种不同类型的基准，覆盖面较广。
    *   **客观性**：论文诚实地展示了 SliceFed 在总吞吐量奖励上可能略低于“无视约束”的基准方案，但强调了其在“安全约束满足”上的绝对优势，这种权衡分析比较客观。

### 6. 主要结论与发现
*   **近乎完美的约束满足**：SliceFed 实现了近 **100% 的 1ms URLLC 延迟达标率**，而启发式方案（QueueProp）在重负载下有约 40% 的丢包。
*   **干扰控制有效**：通过拉格朗日优化，基站能将干扰泄漏精确控制在预设预算（-15 dBm）附近，实现了频谱效率与干扰抑制的平衡。
*   **高稳定性**：相比于 QueueProp 剧烈的资源分配波动，SliceFed 学习到的策略非常平滑，减少了信令开销和系统震荡。
*   **强鲁棒性**：在流量负载增加时，SliceFed 依然能保持约束违反量接近于零，表现出极强的泛化能力。

### 7. 方案优点与亮点
*   **安全性增强**：不同于普通 DRL，SliceFed 显式处理了硬性物理约束，使其更符合电信级应用的要求。
*   **隐私保护**：结合联邦学习，解决了多小区协作中敏感数据不外流的问题。
*   **稳定性设计**：通过重配置成本惩罚和 PPO 裁剪机制，解决了强化学习在无线资源管理中常见的“乒乓效应”。

### 8. 不足与局限性分析
*   **规模局限性**：仿真仅针对 7 个基站的小规模集群，在超大规模（如成百上千基站）网络中的联邦聚合效率和收敛速度尚待验证。
*   **移动性缺失**：实验假设用户位置相对固定或信道变化较慢，未深入探讨高速移动场景下的多普勒效应和频繁切换对模型的影响。
*   **同步开销**：联邦学习依赖同步聚合，在实际网络中可能面临“掉队者”（Stragglers）问题，论文未讨论异步联邦学习的改进。
*   **算力信息缺失**：未提供训练成本数据，难以评估该方案在边缘基站部署时的硬件可行性。

（完）
