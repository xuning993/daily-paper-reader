---
title: Folding Mixed-Integer Linear Programs and Reflection Symmetries
title_zh: 折叠混合整数线性规划与反射对称性
authors: Rolf van der Hulst
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12136v1"
tags: ["query:ml-co"]
score: 9.0
evidence: 混合整数线性规划与分支定界性能
tldr: 针对混合整数线性规划（MILP）和线性规划（LP）中对称性导致的性能下降问题，本文扩展了基于颜色精炼的降维技术（DRCR）。研究不仅将置换对称性推广到反射对称性，还证明了该方法在MILP连续列上的适用性，并利用仿射全单模分解减少整数变量。实验表明，该方法能显著提升SCIP求解器的效率，且算法具有良好的可扩展性。
selection_source: fresh_fetch
motivation: 旨在解决对称性对线性规划和混合整数线性规划求解性能的负面影响，并扩展现有的降维技术。
method: 扩展了颜色精炼算法以识别反射对称性，并结合仿射全单模分解技术对MILP中的整数变量进行重构降维。
result: 在MIPLIB 2017数据集上的测试显示，该方法在LP松弛阶段优于原始DRCR，且能显著缩短MILP的整体求解时间。
conclusion: 改进后的DRCR算法是处理对称性的有效工具，不仅计算速度快，且能有效提升大规模优化问题的求解效率。
---

## 摘要
对于混合整数线性规划和线性规划，众所周知，对称性会对分支定界算法和线性优化算法的性能产生负面影响。处理线性规划中对称性的一种常用策略是通过聚合对称变量来降低线性规划的维度，并求解降维后的线性规划。Grohe、Kersting、Mladenov 和 Selman 在其工作“通过颜色精炼进行降维”（DRCR）中表明，运行快速颜色精炼算法足以检测置换对称性并降低线性规划的维度。我们在两个方向上扩展了 DRCR。首先，我们证明了 DRCR 可以扩展到反射对称性，这是对置换对称性的推广。其次，我们证明了一个广为人知的结果，即 DRCR 可以应用于混合整数线性规划的连续列。为了进一步减少整数变量，我们使用仿射全单模分解将混合整数线性规划重新表述为具有更少整数变量的混合整数线性规划。在 MIPLIB 2017 集合上使用 SCIP 10 进行的计算实验表明，DRCR 是处理对称性的有效工具。对于线性规划松弛，与原始 DRCR 程序相比，具有反射对称性的 DRCR 在运行时间上实现了适度的减少。对于混合整数线性规划模型，与 SCIP 的默认配置相比，DRCR 在缩短求解时间方面非常有效。此外，所开发的 DRCR 检测算法运行迅速，且能很好地扩展到大规模问题实例。

## Abstract
For mixed-integer linear programming and linear programming it is well known that symmetries can have a negative impact on the performance of branch-and-bound and linear optimization algorithms. A common strategy to handle symmetries in linear programs is to reduce the dimension of the linear program by aggregating symmetric variables and solving a linear program of reduced dimension. In their work ``Dimension Reduction via Color Refinement'' (DRCR), Grohe, Kersting, Mladenov and Selman show that it is sufficient to run a fast color refinement algorithm to detect permutation symmetries and reduce the dimension of the linear program. We extend DRCR in two directions. First, we show that DRCR can be extended to reflection symmetries, which generalize permutation symmetries. Second, we show the folklore result that DRCR can be applied to the continuous columns of mixed-integer linear programs. In order to derive additional reductions on the integer variables we use affine totally unimodular decompositions to reformulate mixed-integer linear programs into mixed-integer linear programs with fewer integer variables. Computational experiments on MIPLIB 2017 collection set using SCIP 10 show that DRCR is an effective tool for handling symmetries. For the linear programming relaxations, DRCR with reflection symmetries yields a modest reduction in running time compared to the original DRCR procedure. For mixed-integer linear programming models, DRCR is very effective at reducing the solution time compared to the default configuration of SCIP. Moreover, the developed DRCR detection algorithms are fast and scale well to large problem instances.

---

## 论文详细总结（自动生成）

这是一份关于论文《Folding Mixed-Integer Linear Programs and Reflection Symmetries》（折叠混合整数线性规划与反射对称性）的结构化深入分析报告。

---

### 1. 核心问题与研究背景
*   **核心问题**：在数学优化中，对称性会导致分支定界（Branch-and-Bound）算法重复探索搜索空间的对称部分，严重降低求解效率。
*   **研究背景**：
    *   **线性规划（LP）**：已有技术如 DRCR（通过颜色精炼进行降维）能快速检测置换对称性并聚合变量，但在处理更复杂的“反射对称性”（涉及变量取反或补数）方面存在不足。
    *   **混合整数线性规划（MILP）**：由于整数约束的非凸性，简单的变量聚合往往会破坏整数可行性。现有的“轨道收缩”（Orbital Shrinking）通常只作为松弛手段，需要复杂的子问题来恢复原解。
*   **研究动机**：扩展 DRCR 技术以涵盖反射对称性，并将其安全地应用于 MILP，实现既能减少变量维度又能保持整数解对应关系的“精确降维”。

### 2. 方法论
论文提出了两个主要的扩展方向：

#### A. 反射对称性的折叠（Reflection Folding）
*   **核心思想**：将反射对称性检测转化为置换对称性检测。
*   **关键技术**：
    1.  **中心化（Centering）**：通过仿射变换将变量域的中心移至原点。
    2.  **变量拆分（Variable Splitting）**：将每个变量 $x$ 拆分为正部 $x^+$ 和负部 $x^-$，并复制约束方程的负形式。
    3.  **对称颜色精炼**：在拆分后的模型上运行改进的颜色精炼算法，识别“单极（Unipolar）”和“双极（Bipolar）”分区，从而捕获变量取反后的对称性。

#### B. MILP 的整数变量聚合
*   **核心思想**：利用“仿射全单模（Affine TU）分解”来识别何时聚合整数变量是安全的。
*   **关键技术**：
    1.  **精确轨道收缩**：证明如果聚合后的子问题满足全单模性质，则降维后的整数解一定能映射回原问题的整数解。
    2.  **网络矩阵检测**：开发了基于网络矩阵（Network Matrices）及其转置的快速检测算法，用于识别约束矩阵中的全单模子结构。
    3.  **算法流程**：首先识别连续变量的对称性，然后尝试在不破坏全单模性质的前提下，贪婪地聚合整数变量。

### 3. 实验设计
*   **数据集**：使用国际公认的 **MIPLIB 2017** 集合（包含 1065 个实例）。
*   **场景与 Benchmark**：
    *   **LP 实验**：对比原始 DRCR 与改进后的反射 DRCR（R-DRCR）在处理 LP 松弛模型时的表现。
    *   **MILP 实验**：以开源求解器 **SCIP 10** 的默认配置为基准，对比加入 R-DRCR-N（网络矩阵检测）和 R-DRCR-T（转置网络矩阵检测）后的性能。
*   **预处理**：所有实例先经过 PaPILO 进行标准预处理，以确保对比的公平性。

### 4. 资源与算力
*   **硬件环境**：AMD Ryzen 2600 CPU，16GB 内存。
*   **软件环境**：SCIP 10.0, SoPlex 8.0.0, PaPILO 3.0.0。
*   **算力消耗**：每个实例的求解时限设置为 1 小时。论文未明确给出总训练/测试时长，但强调了检测算法本身的耗时极短（通常在 0.1 到 10 秒之间）。

### 5. 实验数量与充分性
*   **实验规模**：对 1000 多个实例进行了大规模测试，并针对受影响的特定子集（约 80-200 个实例）进行了深入分析。
*   **充分性**：
    *   涵盖了 LP 松弛和完整 MILP 求解两个层面。
    *   使用了“平移几何平均值（Shifted Geometric Mean）”和“原对偶积分（PDI）”等专业指标，能够客观反映求解速度和界限收敛情况。
    *   进行了消融性质的对比（如 N 型 vs T 型检测），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **LP 层面**：反射对称性虽然在实际模型中不如置换对称性常见，但在受影响的实例中，R-DRCR 比原始 DRCR 平均提速约 **27%**。
*   **MILP 层面**：R-DRCR 非常有效。在受影响的 MIPLIB 实例上，其求解速度比 SCIP 默认配置快 **2 倍以上**。
*   **效率与扩展性**：所提出的检测算法具有线性对数级（Linearithmic）的时间复杂度，能够轻松处理拥有数百万行约束的大规模实例。
*   **网络矩阵优势**：R-DRCR-N（网络矩阵）在受影响实例数量和提速效果上均优于 R-DRCR-T。

### 7. 优点
*   **理论完备性**：首次系统地将颜色精炼技术从简单的置换对称性扩展到反射对称性，并为 MILP 的整数变量聚合提供了严格的理论保证。
*   **实用性强**：算法复杂度低，易于集成到现有的商业或开源求解器中。
*   **显著提速**：在高度对称的复杂问题（如广义指派问题）上表现出极强的降维能力。

### 8. 不足与局限
*   **Crossover 问题**：降维后的 LP 解位于原问题最优面的内部，若要获得顶点解（Basic Solution），需要昂贵的 Crossover 过程，这在某些情况下可能抵消降维带来的收益。
*   **全单模约束**：为了保证整数解的对应关系，该方法只能处理符合全单模结构的整数对称性，无法处理所有类型的整数对称性。
*   **数值风险**：在进行中心化仿射变换时，如果变量域非常大，可能会引入数值不稳定性。
*   **覆盖范围**：反射对称性在通用测试集中的占比相对较低（约 10% 的实例受影响），其普适性受限。

（完）
