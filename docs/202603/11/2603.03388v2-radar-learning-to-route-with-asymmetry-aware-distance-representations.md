---
title: "RADAR: Learning to Route with Asymmetry-aware DistAnce Representations"
title_zh: RADAR：利用非对称感知距离表示学习路径规划
authors: "Hang Yi, Ziwei Huang, Yining Ma, Zhiguang Cao"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03388v2"
tags: ["query:ml-co"]
score: 10.0
evidence: 针对具有非对称距离表示的车辆路径问题的神经求解器
tldr: 针对现实世界中车辆路径规划问题（VRP）的非对称性挑战，本文提出了RADAR框架。该框架通过SVD分解初始化紧凑的静态嵌入以捕捉出入成本差异，并利用Sinkhorn归一化改进注意力机制以建模动态非对称交互。实验证明，RADAR在多种非对称VRP任务及真实场景中表现优异，具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的神经求解器多假设对称欧几里得距离，难以有效处理现实中复杂的非对称距离矩阵及其规模化泛化问题。
method: 结合SVD分解提取静态非对称特征，并采用Sinkhorn归一化替代标准Softmax以增强注意力机制中的行列距离感知。
result: 在合成和真实世界的非对称VRP基准测试中，RADAR在分布内和分布外实例上均显著优于现有强基准模型。
conclusion: RADAR为解决非对称路径规划问题提供了一种可扩展且鲁棒的神经框架，显著提升了模型在实际应用中的性能。
---

## 摘要
近期的神经求解器在车辆路径规划问题（VRP）上取得了强劲的性能，但它们主要假设对称的欧几里得距离，这限制了其在现实场景中的适用性。一个核心挑战是如何编码 VRP 非对称距离矩阵中的关系特征。早期的尝试直接对这些矩阵进行编码，但往往无法产生紧凑的嵌入，且在大规模问题上的泛化能力较差。在本文中，我们提出了 RADAR，这是一个可扩展的神经框架，它增强了现有神经 VRP 求解器处理非对称输入的能力。RADAR 从静态和动态两个角度处理非对称性。它利用非对称距离矩阵上的奇异值分解（SVD）来初始化紧凑且具有泛化能力的嵌入，这些嵌入本质上编码了每个节点入站和出站成本中的静态非对称性。为了进一步建模编码过程中嵌入交互里的动态非对称性，它用 Sinkhorn 归一化取代了标准的 softmax，从而在注意力权重中引入了行列联合的距离感知。在各种 VRP 的合成和真实世界基准测试上进行的大量实验表明，RADAR 在分布内和分布外实例上均优于强基准模型，展示了在解决非对称 VRP 方面的鲁棒泛化能力和卓越性能。

## Abstract
Recent neural solvers have achieved strong performance on vehicle routing problems (VRPs), yet they mainly assume symmetric Euclidean distances, restricting applicability to real-world scenarios. A core challenge is encoding the relational features in asymmetric distance matrices of VRPs. Early attempts directly encoded these matrices but often failed to produce compact embeddings and generalized poorly at scale. In this paper, we propose RADAR, a scalable neural framework that augments existing neural VRP solvers with the ability to handle asymmetric inputs. RADAR addresses asymmetry from both static and dynamic perspectives. It leverages Singular Value Decomposition (SVD) on the asymmetric distance matrix to initialize compact and generalizable embeddings that inherently encode the static asymmetry in the inbound and outbound costs of each node. To further model dynamic asymmetry in embedding interactions during encoding, it replaces the standard softmax with Sinkhorn normalization that imposes joint row and column distance awareness in attention weights. Extensive experiments on synthetic and real-world benchmarks across various VRPs show that RADAR outperforms strong baselines on both in-distribution and out-of-distribution instances, demonstrating robust generalization and superior performance in solving asymmetric VRPs.

---

## 论文详细总结（自动生成）

这是一份关于论文《RADAR: Learning to Route with Asymmetry-aware DistAnce Representations》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的神经组合优化（NCO）求解器大多假设车辆路径规划问题（VRP）处于对称的欧几里得空间（基于坐标输入）。然而，现实世界中的交通成本受单行线、拓扑结构和拥堵影响，通常是非对称的。
*   **核心挑战**：如何有效地将非对称距离矩阵的关系特征编码为节点嵌入。现有的方法（如直接编码矩阵或使用近邻信息）往往缺乏紧凑性，且在面对大规模问题或分布偏移时泛化性较差。
*   **论文目标**：提出一个名为 **RADAR** 的可扩展框架，通过捕捉“静态”和“动态”非对称性，增强神经求解器处理现实非对称 VRP 的能力。

### 2. 方法论
RADAR 从两个维度重新设计了编码器：
*   **静态非对称性（SVD 初始化）**：
    *   **核心思想**：利用截断奇异值分解（TSVD）将非对称距离矩阵 $D$ 分解为左奇异向量 $U$、奇异值 $\Sigma$ 和右奇异向量 $V$。
    *   **技术细节**：将节点 $i$ 作为起点（出站）和终点（入站）的角色分别用 $U\sqrt{\Sigma}$ 和 $V\sqrt{\Sigma}$ 表示，拼接后作为初始节点嵌入。这使得模型在初始阶段就能感知节点的全局方向性特征。
*   **动态非对称性（Sinkhorn 归一化）**：
    *   **核心思想**：传统的注意力机制使用行向 Softmax，仅关注节点 $i$ 的局部邻域。
    *   **技术细节**：RADAR 引入 **Sinkhorn 归一化** 替代 Softmax，通过迭代地进行行和列归一化，强制注意力矩阵达到“双随机”（Doubly Stochastic）状态。这确保了注意力权重能同时考虑交互双方（节点 $i$ 和 $j$）的完整邻域结构，从而在表示学习过程中建模动态的非对称交互。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：非对称旅行商问题（ATSP）和非对称带容量限制的 VRP（ACVRP），规模涵盖 100、200、500 到 1000 个节点。
    *   **多任务场景**：在 16 种不同的 VRP 变体（如带时间窗、开放路径、回程运输等）上进行测试。
    *   **真实世界数据**：使用基于 OpenStreetMap 提取的 100 个城市的真实路网数据（RRNCO 数据集）。
*   **对比方法（Baselines）**：
    *   **传统求解器**：LKH3、HGS、OR-Tools。
    *   **神经求解器**：MatNet、ICAM、ReLD、UNICO、RRNCO 等。
*   **评估指标**：目标函数值（Obj.）、差距（Gap）、推理时间。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3090 GPU (24 GB)**。
*   **训练时长**：
    *   ATSP 训练耗时约 **39.31 小时**。
    *   ACVRP 训练耗时约 **54.74 小时**。
*   **效率优化**：相比 MatNet，RADAR 通过减少注意力头数和优化训练周期，实现了约 89% 的训练时间缩减。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 17 种合成变体和 3 种真实世界任务。
*   **消融实验**：针对 SVD 初始化（对比了 EVD、MDS、QR 分解等）、Sinkhorn 迭代次数、坐标的影响、不同需求分布等进行了深入分析。
*   **客观性与公平性**：所有基准模型均在相同设置下重新训练或使用官方检查点；通过 1000 个实例的平均性能和统计显著性测试（t-test）确保结果可靠；讨论了 Z-score 归一化对所有模型的影响，确保对比公平。

### 6. 主要结论与发现
*   **性能卓越**：RADAR 在所有非对称 VRP 变体中均优于现有的神经求解器，且在 ACVRP200 等任务上甚至超过了传统强力求解器 LKH。
*   **泛化能力强**：在 100 规模上训练的模型，在 500 和 1000 规模的零样本泛化（Zero-shot Generalization）中表现稳健，Gap 增长远低于基准模型。
*   **坐标并非必须**：实验证明，在非对称任务中，距离矩阵比坐标包含更关键的信息，RADAR 即使不使用坐标也能超越使用坐标增强的其他模型。

### 7. 优点
*   **数学支撑扎实**：将 SVD 引入 NCO 初始化，为非对称关系的节点表示提供了理论依据。
*   **即插即用**：Sinkhorn 注意力机制可以轻松集成到现有的 Transformer 架构中。
*   **现实适用性**：直接处理距离矩阵而非依赖欧几里得假设，使其更接近物流配送等实际应用场景。

### 8. 不足与局限
*   **计算开销**：虽然论文提到 SVD 在 GPU 上有加速，但对于超大规模（如数万节点）的实时动态更新场景，SVD 的重新计算可能成为瓶颈。
*   **内存占用**：Sinkhorn 归一化涉及多次迭代，在大规模实例的训练阶段可能会增加显存压力。
*   **应用限制**：目前主要针对静态距离矩阵，对于实时剧烈波动的路况（动态 VRP）的适应性尚待进一步验证。

（完）
