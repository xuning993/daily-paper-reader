---
title: "RADAR: Learning to Route with Asymmetry-aware DistAnce Representations"
title_zh: RADAR：利用非对称感知距离表示学习路径规划
authors: "Hang Yi, Ziwei Huang, Yining Ma, Zhiguang Cao"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03388v1"
tags: ["query:ml-co"]
score: 10.0
evidence: 针对非对称距离车辆路径问题的神经求解器
tldr: 针对现有神经车辆路径规划（VRP）求解器在处理非对称距离矩阵时表现不佳的问题，本文提出了RADAR框架。该框架通过SVD分解初始化紧凑的静态非对称嵌入，并利用Sinkhorn归一化在注意力机制中建模动态非对称交互。实验证明，RADAR在多种非对称VRP任务中表现优异，具有极强的泛化能力和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的神经VRP求解器大多假设对称欧几里得距离，难以有效处理现实世界中复杂的非对称距离矩阵。
method: 结合SVD分解提取静态出入成本特征，并采用Sinkhorn归一化替代标准Softmax以增强注意力机制对动态非对称性的感知。
result: 在合成和真实世界的非对称VRP基准测试中，RADAR在分布内和分布外实例上均显著优于现有基准模型。
conclusion: RADAR为神经求解器处理非对称路径规划问题提供了一种可扩展且高效的表征学习方案。
---

## 摘要
近期的神经求解器在车辆路径规划问题（VRP）上取得了强劲的性能，但它们主要假设对称的欧几里得距离，这限制了其在现实场景中的适用性。一个核心挑战是如何编码 VRP 非对称距离矩阵中的关系特征。早期的尝试直接对这些矩阵进行编码，但往往无法产生紧凑的嵌入，且在大规模问题上的泛化能力较差。在本文中，我们提出了 RADAR，这是一个可扩展的神经框架，它增强了现有神经 VRP 求解器处理非对称输入的能力。RADAR 从静态和动态两个角度处理非对称性。它利用非对称距离矩阵上的奇异值分解（SVD）来初始化紧凑且具有泛化能力的嵌入，这些嵌入本质上编码了每个节点入站和出站成本中的静态非对称性。为了进一步建模编码过程中嵌入交互里的动态非对称性，它用 Sinkhorn 归一化取代了标准的 softmax，从而在注意力权重中引入了行列联合的距离感知。在各种 VRP 的合成和真实世界基准测试上进行的大量实验表明，RADAR 在分布内和分布外实例上均优于强基准模型，展示了在解决非对称 VRP 方面的鲁棒泛化能力和卓越性能。

## Abstract
Recent neural solvers have achieved strong performance on vehicle routing problems (VRPs), yet they mainly assume symmetric Euclidean distances, restricting applicability to real-world scenarios. A core challenge is encoding the relational features in asymmetric distance matrices of VRPs. Early attempts directly encoded these matrices but often failed to produce compact embeddings and generalized poorly at scale. In this paper, we propose RADAR, a scalable neural framework that augments existing neural VRP solvers with the ability to handle asymmetric inputs. RADAR addresses asymmetry from both static and dynamic perspectives. It leverages Singular Value Decomposition (SVD) on the asymmetric distance matrix to initialize compact and generalizable embeddings that inherently encode the static asymmetry in the inbound and outbound costs of each node. To further model dynamic asymmetry in embedding interactions during encoding, it replaces the standard softmax with Sinkhorn normalization that imposes joint row and column distance awareness in attention weights. Extensive experiments on synthetic and real-world benchmarks across various VRPs show that RADAR outperforms strong baselines on both in-distribution and out-of-distribution instances, demonstrating robust generalization and superior performance in solving asymmetric VRPs.

---

## 论文详细总结（自动生成）

以下是对论文《RADAR: Learning to Route with Asymmetry-aware DistAnce Representations》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的神经组合优化（NCO）求解器大多假设节点分布在欧几里得空间且距离对称。然而，现实世界的物流路径受单行道、交通拥堵等因素影响，距离矩阵通常是非对称的。
*   **核心挑战**：如何在神经网络中有效编码非对称距离矩阵的关系特征。现有的方法（如直接输入矩阵或使用坐标）要么难以捕捉全局结构，要么在处理非对称性时缺乏紧凑的节点表征，导致在大规模问题上泛化性差。
*   **研究目标**：提出一个可扩展的框架，使神经求解器能够感知并利用距离矩阵中的“静态”和“动态”非对称性。

### 2. 方法论：核心思想与关键技术
RADAR 框架通过两个关键组件处理非对称性：
*   **静态非对称性：基于 SVD 的嵌入初始化**
    *   **核心思想**：利用截断奇异值分解（TSVD）将非对称距离矩阵 $D$ 分解为左、右奇异向量。
    *   **技术细节**：计算 $D \approx U_k \Sigma_k V_k^\top$。将左因子（代表节点作为起点的特征）和右因子（代表节点作为终点的特征）进行拼接，形成初始节点嵌入。
    *   **优势**：这种初始化方式在理论上能通过线性变换还原原始非对称矩阵，且不依赖于节点坐标，具有更强的结构感知能力。
*   **动态非对称性：Sinkhorn 归一化注意力机制**
    *   **核心思想**：用 Sinkhorn 归一化取代编码器中标准的行向 Softmax。
    *   **技术细节**：Sinkhorn 通过迭代进行行列双向归一化，使注意力矩阵变为双标定随机矩阵（Doubly Stochastic）。
    *   **优势**：这强制模型在计算注意力时同时考虑节点 $i$ 的邻域和节点 $j$ 的邻域，从而在表征学习过程中捕捉更复杂的双向交互流。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：非对称旅行商问题（ATSP）和非对称容量限制车辆路径问题（ACVRP），规模涵盖 100 到 1000 个节点。
    *   **多任务场景**：在 16 种不同的非对称 VRP 变体（如带时间窗、带回程等）上进行多任务学习。
    *   **真实世界数据**：使用基于 RRNCO 的真实城市路网数据集（包含 100 个城市的实际行驶距离和时间）。
*   **Benchmark 与对比方法**：
    *   **传统求解器**：LKH3、HGS、OR-Tools。
    *   **神经求解器**：MatNet、ICAM、UNICO、ELG、ReLD、RRNCO 等最先进（SOTA）模型。
*   **评估指标**：目标函数值（Obj.）、差距（Gap）、推理时间。

### 4. 资源与算力
*   **硬件环境**：单张 NVIDIA RTX 3090 GPU (24 GB)。
*   **训练时长**：
    *   ATSP 任务训练约 39.31 小时。
    *   ACVRP 任务训练约 54.74 小时。
*   **效率说明**：相比于 MatNet，RADAR 通过减少注意力头数和优化训练轮数，实现了约 89% 的训练时间缩减。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 17 种合成变体和 3 种真实世界任务。
*   **泛化性测试**：在 100 规模上训练，直接在 200、500、1000 规模上进行零样本（Zero-shot）泛化测试。
*   **消融实验**：针对 SVD 初始化、Sinkhorn 归一化、奇异值数量 $k$、坐标的影响、不同非对称程度以及不同需求分布等进行了深入的消融研究。
*   **客观性**：通过多次独立运行的统计显著性测试（t-test）和不同归一化方式的对比，确保了结果的公平性和客观性。

### 6. 主要结论与发现
*   **性能卓越**：RADAR 在所有合成和真实世界基准测试中均显著优于现有的神经求解器，且在某些 ACVRP 规模上甚至超过了传统启发式算法 LKH。
*   **泛化能力强**：SVD 初始化提供了稳定的结构先验，使得模型在面对训练集之外的大规模实例时，性能下降远小于基准模型。
*   **非对称感知的重要性**：实验证明，同时处理静态（初始化）和动态（注意力交互）非对称性是提升性能的关键，缺一不可。
*   **坐标并非必须**：在非对称任务中，距离矩阵比坐标包含更本质的信息，RADAR 证明了仅靠距离矩阵也能达到甚至超越结合坐标的模型。

### 7. 优点
*   **理论支撑**：为“非对称感知嵌入”给出了形式化定义，并证明了 SVD 初始化的合理性。
*   **架构通用**：RADAR 作为一个增强框架，可以轻松集成到现有的各种基于注意力的 VRP 求解器中。
*   **现实意义**：直接针对非对称距离矩阵建模，极大地提升了神经求解器在真实物流场景（如单行道、复杂路网）中的实用价值。

### 8. 不足与局限
*   **计算开销**：虽然 SVD 和 Sinkhorn 的开销在可接受范围内，但对于超大规模矩阵（如数万节点），SVD 的计算成本和内存占用仍可能成为瓶颈。
*   **构造式限制**：RADAR 本质上仍属于构造式求解器，虽然性能强劲，但在极高精度要求下，可能仍需结合搜索或改进算法（Improvement heuristics）来进一步缩小 Gap。
*   **应用范围**：目前主要集中在路径规划类问题，对于其他类型的组合优化问题（如调度、装箱）的适用性有待进一步验证（尽管论文在附录中简要测试了 FFSP 问题）。

（完）
