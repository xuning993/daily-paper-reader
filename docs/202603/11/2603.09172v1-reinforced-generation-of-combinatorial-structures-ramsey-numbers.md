---
title: "Reinforced Generation of Combinatorial Structures: Ramsey Numbers"
title_zh: 组合结构的强化生成：拉姆齐数
authors: "Ansh Nagda, Prabhakar Raghavan, Abhradeep Thakurta"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09172v1"
tags: ["query:ml-co"]
score: 9.0
evidence: 基于大语言模型的智能体用于组合拉姆齐数
tldr: "本研究利用基于大语言模型（LLM）的代码演化智能体 AlphaEvolve，针对组合数学中的经典难题——拉姆齐数（Ramsey Numbers）的下界进行了探索。AlphaEvolve 作为一个通用的元算法，能够自动生成并优化搜索算法，成功提高了 R(3, 13)、R(4, 15) 等 5 个经典拉姆齐数的下界。此外，该方法还复现了所有已知的精确下界，并匹配了多项当前最佳结果，证明了 LLM 在处理复杂组合结构生成任务中的巨大潜力。"
selection_source: fresh_fetch
motivation: 传统的拉姆齐数下界搜索依赖于针对特定问题手工设计的搜索算法，缺乏通用性且难以进一步突破。
method: 提出 AlphaEvolve，一种利用大语言模型进行代码变异和演化的智能体，用于自动生成高效的组合结构搜索算法。
result: 成功提升了 5 个经典拉姆齐数的下界，并验证了该方法在复现已知精确值和匹配现有最佳下界方面的有效性。
conclusion: 研究表明，基于 LLM 的元算法可以替代传统的定制化搜索算法，成为解决复杂组合数学问题的强有力工具。
---

## 摘要
我们提出了五个经典拉姆齐数 (Ramsey numbers) 的改进下界：$\mathbf{R}(3, 13)$ 从 60 增加到 61，$\mathbf{R}(3, 18)$ 从 99 增加到 100，$\mathbf{R}(4, 13)$ 从 138 增加到 139，$\mathbf{R}(4, 14)$ 从 147 增加到 148，以及 $\mathbf{R}(4, 15)$ 从 158 增加到 159。这些结果是使用 AlphaEvolve 实现的，它是一个基于大语言模型 (LLM) 的代码变异智能体。除了这些新结果外，我们还成功恢复了所有已知精确值的拉姆齐数的下界，并在许多其他情况下达到了已知的最佳下界。其中包括一些先前工作未详细说明所用算法的下界。实际上，几乎所有已知的拉姆齐数下界都是通过计算得出的，每种定制的搜索算法仅能提供少数结果。AlphaEvolve 是一种单一的元算法，能够为我们所有的结果生成相应的搜索算法。

## Abstract
We present improved lower bounds for five classical Ramsey numbers: $\mathbf{R}(3, 13)$ is increased from $60$ to $61$, $\mathbf{R}(3, 18)$ from $99$ to $100$, $\mathbf{R}(4, 13)$ from $138$ to $139$, $\mathbf{R}(4, 14)$ from $147$ to $148$, and $\mathbf{R}(4, 15)$ from $158$ to $159$. These results were achieved using~\emph{AlphaEvolve}, an LLM-based code mutation agent. Beyond these new results, we successfully recovered lower bounds for all Ramsey numbers known to be exact, and matched the best known lower bounds across many other cases. These include bounds for which previous work does not detail the algorithms used. Virtually all known Ramsey lower bounds are derived computationally, with bespoke search algorithms each delivering a handful of results. AlphaEvolve is a single meta-algorithm yielding search algorithms for all of our results.

---

## 论文详细总结（自动生成）

这篇论文展示了人工智能（特别是大语言模型）在组合数学这一高度抽象且困难的领域中的突破性应用。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **核心问题**：寻找经典拉姆齐数 $R(r, s)$ 的更优下界。拉姆齐数 $R(r, s)$ 是指最小的整数 $n$，使得任何 $n$ 个顶点的图都必然包含一个大小为 $r$ 的团（clique）或一个大小为 $s$ 的独立集（independent set）。
*   **研究背景**：确定拉姆齐数的精确值极其困难。目前大多数下界是通过针对特定 $(r, s)$ 对手工设计的搜索算法（如模拟退火、禁忌搜索等）计算得出的。
*   **整体含义**：本研究证明了无需为每个问题设计特定的算法，通过一个基于大语言模型（LLM）的“元算法”——**AlphaEvolve**，可以自动演化出能够发现极端组合结构的搜索程序。

### 2. 论文提出的方法论
*   **核心思想**：利用 AlphaEvolve 智能体，通过 LLM 迭代地变异和演化代码片段，从而生成能够构建更大规模“无 $(r, s)$ 结构”图的搜索算法。
*   **关键技术细节**：
    *   **演化机制**：维护一个搜索算法种群 $P$。使用性能导向的选择函数挑选父代，由 LLM 生成变异版本 $p_{new}$。
    *   **双图评估**：执行 $p_{new}$ 会产生两个图：主图 $G_1$（用于验证是否满足约束）和更大的“前景图” $G_2$（用于评估潜力）。
    *   **评分系统**：如果 $G_1$ 有效，根据其顶点数评分（超过当前最佳下界时给予重奖）。同时，根据 $G_2$ 中违反约束的数量（团和独立集的计数）给予额外奖励，引导算法向减少冲突的方向演化。
    *   **初始化策略**：AlphaEvolve 自动发现了四类初始化方法：随机/随机初始化、Paley/代数种子、循环/循环引导、以及混合/分形/谱种子。

### 3. 实验设计
*   **场景与目标**：针对 $r, s \le 22$ 的小规模拉姆齐数。
*   **Benchmark（基准）**：以 Stanisław Radziszowski 维护的权威综述《Small Ramsey Numbers》中的当前最佳下界（SoTA）为基准。
*   **对比方法**：对比了历史上由 Exoo、Tatarevic 等专家设计的各种定制化搜索算法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：文中提到了“在计算预算剩余的情况下”运行演化循环，并使用了 Google 的 Gemini 模型来辅助总结生成的代码。由于作者来自 Google DeepMind 和 UC Berkeley，可以推测其背后依托了强大的计算集群资源。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了拉姆齐数表中的 28 个单元格（见表 1）。
*   **充分性**：
    *   **新发现**：成功提升了 5 个拉姆齐数的下界（$R(3,13), R(3,18), R(4,13), R(4,14), R(4,15)$）。
    *   **复现性**：成功匹配了所有已知精确值的下界，并达到了许多其他单元格的当前最佳水平。
    *   **客观性**：实验结果通过公开代码和构造出的图（Github 链接）供社区验证，具有很高的客观性。

### 6. 论文的主要结论与发现
*   **性能突破**：AlphaEvolve 作为一个单一的元算法，在多个案例中超越了人类专家数十年积累的定制算法。
*   **算法多样性**：LLM 能够根据不同的 $(r, s)$ 自动选择合适的数学结构（如 Paley 图、循环图等）作为起点，并结合复杂的启发式搜索（如禁忌搜索、自适应模拟退火）。
*   **自动化潜力**：证明了 LLM 驱动的代码搜索可以自动化科学发现过程，减少了对特定领域专家手工设计算法的依赖。

### 7. 优点
*   **通用性强**：AlphaEvolve 不仅适用于拉姆齐数，其框架可推广至其他极端组合数学问题。
*   **启发式创新**：生成的算法往往结合了多种人类未曾尝试过的启发式策略（如“分形镜像初始化”或“谐波隧道效应”）。
*   **结果显著**：在拉姆齐数这种研究极其充分的领域依然能取得 5 项新突破，证明了方法的有效性。

### 8. 不足与局限
*   **仅限下界**：该方法只能通过构造实例来提高下界，无法处理证明非存在性的上界问题（上界通常需要形式化证明或穷举搜索）。
*   **算法迁移性差**：AlphaEvolve 为特定单元格生成的算法往往无法直接迁移到其他单元格，每个问题仍需独立的演化过程。
*   **算力黑盒**：缺乏算力消耗的详细数据，使得其他研究者难以评估复现该结果所需的成本。
*   **依赖初始化**：虽然能自动发现初始化策略，但对于某些极难的问题，可能仍需特定的代数结构作为种子才能取得突破。

（完）
