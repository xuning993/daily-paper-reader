---
title: "SynthCharge: An Electric Vehicle Routing Instance Generator with Feasibility Screening to Enable Learning-Based Optimization and Benchmarking"
title_zh: SynthCharge：一种具备可行性筛选功能的电动汽车路径规划实例生成器，旨在支持基于学习的优化与基准测试
authors: "Mertcan Daysalilar, Fuat Uyguroglu, Gabriel Nicolosi, Adam Meyers"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03230v1"
tags: ["query:ml-co"]
score: 9.0
evidence: 用于基于学习的路径优化实例生成器
tldr: SynthCharge是一个针对带时间窗的电动汽车路径规划问题（EVRPTW）的参数化实例生成器。它解决了现有基准数据集静态且缺乏可行性验证的问题，通过集成几何特征、自适应能量缩放和充电站布局，并结合快速可行性筛选，生成多样化且可解的实例。该工具支持5至500个客户规模，为评估基于学习的优化算法提供了稳健的基准测试基础设施。
selection_source: fresh_fetch
motivation: 现有的EVRPTW基准数据集通常是静态的且无法保证实例的可解性，限制了对基于学习的路由模型的评估。
method: 开发了一个名为SynthCharge的参数化生成器，通过自适应能量缩放、充电站布局和快速可行性筛选流程来生成多样化的实例。
result: 成功实现了可扩展至500个客户规模的实例生成，并重点在5至100个客户的规模上验证了其生成多样化且结构有效实例的能力。
conclusion: SynthCharge为神经路由和数据驱动方法提供了必要的动态基准测试基础设施，有助于系统地评估算法的鲁棒性。
---

## 摘要
带时间窗的电动汽车路径规划问题 (EVRPTW) 通过引入电池容量限制和充电站决策，扩展了经典的 VRPTW。现有的基准数据集通常是静态的，且缺乏可验证的可行性，这限制了对基于学习的路径规划模型进行可重复的评估。我们推出了 SynthCharge，这是一种参数化生成器，能够在不同的时空配置和可扩展的客户数量下，生成经过可行性筛选的多样化 EVRPTW 实例。虽然 SynthCharge 目前可以生成多达 500 个客户的大规模实例，但我们的实验重点关注 5 到 100 个客户的规模。与静态基准套件不同，SynthCharge 将实例几何结构与自适应能量容量缩放以及感知范围的充电站布局相结合。为了保证结构有效性，该生成器通过快速可行性筛选过程系统地过滤掉不可解的实例。最终，SynthCharge 为系统评估新兴神经路径规划和数据驱动方法的鲁棒性提供了所需的动态基准测试基础设施。

## Abstract
The electric vehicle routing problem with time windows (EVRPTW) extends the classical VRPTW by introducing battery capacity constraints and charging station decisions. Existing benchmark datasets are often static and lack verifiable feasibility, which restricts reproducible evaluation of learning-based routing models. We introduce SynthCharge, a parametric generator that produces diverse, feasibility-screened EVRPTW instances across varying spatiotemporal configurations and scalable customer counts. While SynthCharge can currently generate large-scale instances of up to 500 customers, we focus our experiments on sizes ranging from 5 to 100 customers. Unlike static benchmark suites, SynthCharge integrates instance geometry with adaptive energy capacity scaling and range-aware charging station placement. To guarantee structural validity, the generator systematically filters out unsolvable instances through a fast feasibility screening process. Ultimately, SynthCharge provides the dynamic benchmarking infrastructure needed to systematically evaluate the robustness of emerging neural routing and data-driven approaches.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SynthCharge** 的参数化实例生成器，专门用于带时间窗的电动汽车路径规划问题（EVRPTW）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着城市货运电动化，EVRPTW 成为智能交通系统（ITS）的核心问题。与传统路径规划相比，它增加了电池容量限制和充电决策。
*   **核心问题**：
    *   **基准数据集局限性**：现有的 EVRPTW 基准集（如基于 Solomon 的扩展集）通常是静态且有限的，缺乏参数化控制。
    *   **可行性缺失**：随机生成的实例往往包含不可解的情况（如客户超出续航范围），这会干扰基于学习（如强化学习）的模型的训练与评估。
    *   **泛化性挑战**：神经路径规划模型对分布偏移（如空间结构、约束紧度变化）非常敏感，需要能系统改变特征的动态基准测试工具。
*   **研究意义**：SynthCharge 旨在提供一个可控、可验证、可扩展的实例生成基础设施，以支持数据驱动优化算法的鲁棒性评估。

### 2. 核心方法论
SynthCharge 的核心是一个集成了几何感知和可行性筛选的生成流程：
*   **空间拓扑生成**：支持三种模式：随机（Random）、聚类（Clustered）和混合（Mixed），模拟不同的城市配送场景。
*   **感知范围的充电站布局**：
    *   **客户间可达性**：若两客户距离超过续航 $R$ 的 80%，则在其中心点附近插入充电站。
    *   **仓库可达性**：若客户距离仓库过远且附近无站，则在射线路径上添加充电站。
*   **自适应能量缩放**：电池容量 $B$ 不是固定的，而是根据实例的最大空间跨度动态调整，确保实例既有能量约束又是潜在可达的。
*   **时间窗分配**：通过宽度比例参数 $\phi$ 控制时间窗的紧度（宽、中、紧），并采用交错的起始时间。
*   **两阶段可行性筛选**：
    *   **第一阶段（结构筛选）**：线性时间内检查必要条件（如每个客户是否在充电站覆盖范围内、是否能在时限内返回仓库）。
    *   **第二阶段（精确验证）**：针对小规模实例（$N \le 10$），调用混合整数线性规划（MILP）求解器确保绝对可行。

### 3. 实验设计
*   **实验场景**：涵盖了 5 到 100 个客户规模（5C2S 到 100C12S），共 9 种时空组合（3 种空间分布 $\times$ 3 种时间窗紧度）。
*   **验证基准**：
    *   使用**接受率（Acceptance Rate, $\gamma$）**衡量生成效率和约束相互作用。
    *   使用**基准元启发式算法（VNS/Tabu Search）**对生成的实例进行实际求解，验证其可行性。
*   **对比维度**：分析了不同时空制度下，接受率的变化以及对行驶总里程和所需车辆数的影响。

### 4. 资源与算力
*   **硬件环境**：第 13 代 Intel Core i7-13700K 处理器（16 核，3.40 GHz），64 GB RAM。
*   **算力消耗**：
    *   生成效率极高：对于 $N > 10$ 的实例（仅用第一阶段筛选），生成每个可行实例的时间小于 0.01 秒。
    *   对于 $N \le 10$ 的实例（包含 MILP 验证），生成时间约为 0.06 - 0.10 秒。
    *   文中未提及 GPU 使用，因为该工具主要用于 CPU 端的实例生成而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：共生成了 **2,475 个可行实例**（每个规模/制度组合 25 个）。
*   **充分性评价**：实验设计较为充分。作者不仅测试了生成器的速度，还通过元启发式算法 100% 的求解成功率证明了筛选机制的有效性。实验覆盖了从极小规模到中等规模的多种约束组合，能够客观反映不同参数对问题难度的影响。

### 6. 主要结论与发现
*   **可行性筛选的必要性**：简单的随机生成会导致大量不可解实例，尤其是在聚类分布和紧时间窗制度下，接受率显著下降。
*   **约束耦合效应**：时间窗的收紧会显著增加所需的车队规模和行驶里程，且这种影响在随机分布中比聚类分布更明显。
*   **生成器性能**：SynthCharge 能够快速生成大规模（最高支持 500 客户）且保证结构有效的实例，适合作为深度学习训练的数据源。

### 7. 优点
*   **动态性与可控性**：解决了静态基准集无法进行分布偏移测试的问题。
*   **自适应缩放**：能量容量与几何结构挂钩，避免了因空间尺度变化导致的平凡解（能量过多）或无解（能量过少）。
*   **透明度**：每个实例附带 JSON 元数据，记录了完整的生成参数和可行性状态，符合机器学习领域对数据集透明度的要求。

### 8. 不足与局限
*   **精确验证规模受限**：由于 MILP 的计算复杂度，精确可行性验证仅限于 $N \le 10$ 的实例，大规模实例仍可能存在结构筛选无法发现的深层不可解性。
*   **模型简化**：目前仅支持欧几里得空间、单仓库、线性充电模型和确定性行驶时间，未考虑非线性充电曲线、多仓库或交通拥堵等现实复杂因素。
*   **应用范围**：目前主要针对学术基准测试，尚未集成真实路网地图数据。

（完）
