---
title: Preference-Conditioned Reinforcement Learning for Space-Time Efficient Online 3D Bin Packing
title_zh: 偏好调节的强化学习用于时空高效的在线三维装箱
authors: "Nikita Sarawgi, Omey M. Manyar, Fan Wang, Thinh H. Nguyen, Daniel Seita, Satyandra K. Gupta"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07800v1"
tags: ["query:ml-co"]
score: 10.0
evidence: 强化学习用于3D装箱优化
tldr: "本研究针对自动化仓储中的机器人装箱问题，提出了一种名为 STEP 的时空高效装箱方法。针对现有系统在空间利用率与执行速度之间难以平衡的挑战，该方法采用基于 Transformer 的偏好条件强化学习策略，通过显式权衡候选动作的装箱收益与操作时间，实现了灵活的决策。STEP 不仅能泛化至不同规模的候选集，还在保持装箱密度的同时将操作时间降低了 44%，为高效在线 3D 装箱提供了新方案。"
selection_source: fresh_fetch
motivation: 现有的机器人装箱系统在追求高空间利用率时往往忽略了操作时间成本，需要在紧凑放置与执行速度之间取得平衡。
method: 提出 STEP 方法，利用基于 Transformer 的偏好条件强化学习策略，在每一步评估多个候选动作并权衡预期的装箱收益与操作时间。
result: "在不牺牲装箱密度的前提下，该方法成功将操作时间减少了 44%，并展现出对不同候选集大小的良好泛化能力。"
conclusion: 通过显式建模时空权衡，偏好条件强化学习能有效提升在线 3D 装箱任务的综合效率。
---

## 摘要
机器人装箱广泛应用于仓库自动化，现有系统通过启发式和基于学习的策略实现了稳健的性能。这些系统必须在紧凑放置与快速执行之间取得平衡，其中选择替代物品或重新调整其方向可以提高空间利用率，但会引入额外的时间。我们提出了一种基于选择的公式化方法，明确地对这种权衡进行推理：在每一步中，机器人评估多个候选动作，权衡预期的装箱收益与估计的操作时间。这使得时间感知策略能够有选择地接受增加的操作时间，前提是它能带来显著的空间改进。我们的方法 STEP（Space-Time Efficient Packing，时空高效装箱）使用了一种基于偏好调节、基于 Transformer 的强化学习策略，并支持在不同候选集大小之间进行泛化以及与标准放置模块的集成。该方法在不牺牲装箱密度的前提下，实现了 44% 的操作时间减少。更多材料见 https://step-packing.github.io。

## Abstract
Robotic bin packing is widely deployed in warehouse automation, with current systems achieving robust performance through heuristic and learning-based strategies. These systems must balance compact placement with rapid execution, where selecting alternative items or reorienting them can improve space utilization but introduce additional time. We propose a selection-based formulation that explicitly reasons over this trade-off: at each step, the robot evaluates multiple candidate actions, weighing expected packing benefit against estimated operational time. This enables time-aware strategies that selectively accept increased operational time when it yields meaningful spatial improvements. Our method, STEP (Space-Time Efficient Packing), uses a preference-conditioned, Transformer-based reinforcement learning policy, and allows generalization across candidate set sizes and integration with standard placement modules. It achieves a 44% reduction in operational time without compromising packing density. Additional material is available at https://step-packing.github.io.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **STEP (Space-Time Efficient Packing)** 的方法，旨在解决机器人在线三维装箱（3D-BPP）中空间利用率与操作时间之间的权衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的在线三维装箱研究主要关注**空间利用率（装箱密度）**，而忽略了**操作时间（吞吐量）**。在实际工业场景中，为了获得更好的放置位置，机器人可能需要对物体进行复杂的重新定向（Reorientation）或从非顶面抓取，这会显著增加机械臂的运动时间和失败风险。
*   **研究动机**：现有的系统往往在“放得紧”和“放得快”之间走极端。作者希望建立一个能够根据用户偏好动态调节“空间-时间”权衡的智能决策系统，使机器人能像人类一样，在必要时为了显著的空间收益才牺牲时间。

### 2. 方法论
*   **核心思想**：将装箱问题建模为**多目标马尔可夫决策过程（MOMDP）**，通过**偏好调节（Preference-Conditioned）**的强化学习，让单一策略能够覆盖从“极速装箱”到“极致密度”的整个帕累托前沿（Pareto Front）。
*   **关键技术细节**：
    *   **动作空间扩展**：不仅选择缓冲区中的物品，还选择抓取面（Top, Front, Back, Left, Right）。不同的抓取面对应不同的放置姿态和操作时间成本。
    *   **状态表示**：状态包含箱体剩余空间（EMS编码）、缓冲区物品几何属性、各动作的估计操作时间，以及当前的**偏好向量 $\omega$**（用于平衡空间收益和时间成本）。
    *   **Transformer-Select 架构**：使用 Transformer 编码器处理物品和箱体特征。通过自注意力（Self-Attention）捕捉物品间的关联，通过交叉注意力（Cross-Attention）将物品特征与箱体空间状态对齐。
    *   **算法流程**：采用 **RDP-MORL（稳健动态偏好多目标强化学习）** 框架结合 **PPO** 算法。Critic 网络输出向量化的价值估计（空间和时间各自的预期回报），Actor 根据偏好向量 $\omega$ 对优势函数进行标量化处理，从而优化策略。

### 3. 实验设计
*   **数据集与场景**：使用标准的 **RS 数据集**（包含各种尺寸的随机长方体）。
*   **Benchmark 与对比方法**：
    *   **TopFaceSpace**：仅抓取顶面，仅优化空间（传统基准）。
    *   **ReorientSpace**：允许重新定向，但仅优化空间。
    *   **ReorientTime**：允许重新定向，但仅优化时间。
    *   **MCTS-5**：基于蒙特卡洛树搜索的选择策略。
    *   **GOPT 变体**：对比了直接使用 Transformer 进行放置和选择的端到端模型。
*   **评估指标**：空间利用率（Uti）、总操作时间（Time）、装箱数量（Num）以及抓取面的分布比例。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到使用 PyTorch 实现，基于 Tianshou 强化学习库和 morl-baselines 库。训练时每个回合开始前会随机采样 50 个预定义的偏好权重。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**帕累托前沿分析**，验证了策略在不同偏好下的连续调节能力。
    *   **泛化性实验**：在缓冲区大小（1, 3, 5）上进行了交叉验证（训练 5，测试 1/3/5）。
    *   **消融/鲁棒性实验**：针对物品几何形状的变异性（均匀 vs. 变异形状）进行了对比。
    *   **真实世界验证**：在 ABB IRB 2600 机器人上进行了实物装箱实验。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到实物、从单一目标到多目标的对比，能够客观证明该方法在提升效率方面的有效性。

### 6. 主要结论与发现
*   **显著的时间节省**：与仅优化空间的策略（ReorientSpace）相比，STEP 在仅损失约 1.5% 空间利用率的情况下，**减少了 44% 到 49% 的操作时间**。
*   **帕累托优化**：STEP 成功找到了比传统启发式或单目标学习方法更优的帕累托前沿，证明了显式建模时间成本的必要性。
*   **灵活性**：通过调节偏好向量，系统可以在不重新训练的情况下，适应不同的生产节奏需求（如高峰期追求速度，平稳期追求密度）。

### 7. 优点与亮点
*   **多目标融合**：首次在在线 3D 装箱中将物理操作时间作为显式的优化目标，而非仅仅关注几何填充。
*   **架构先进**：Transformer-Select 模块有效地处理了物品候选集与箱体空间之间的复杂匹配关系。
*   **实用性强**：支持动态偏好调节，且在真实机器人实验中表现出与仿真一致的趋势，具有较强的工业应用潜力。

### 8. 不足与局限
*   **时间成本简化**：实验中的操作时间（如不同表面的抓取难度）被抽象为固定的标量惩罚，实际场景中这些时间可能随机械臂路径规划动态变化。
*   **物体多样性限制**：目前仅针对硬质长方体（Cuboidal items），未深入探讨非规则形状或易碎/易变形物体的特殊约束。
*   **静态稳定性假设**：虽然考虑了重力稳定性，但未详细讨论高速操作下物体的惯性对装箱稳定性的影响。

（完）
