---
title: Constraint-Aware Generative Re-ranking for Multi-Objective Optimization in Advertising Feeds
title_zh: 广告信息流中多目标优化的约束感知生成式重排序
authors: "Chenfei Li, Hantao Zhao, Weixi Yao, Ruiming Huang, Rongrong Lu, Geng Tian, Dongying Kong"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04227v1"
tags: ["query:ml-co"]
score: 9.0
evidence: 使用神经解码解决广告流中的约束组合问题
tldr: 针对广告信息流重排序中的多目标优化和约束处理难题，本文提出了一种约束感知生成式重排序框架。该框架将约束优化转化为有界神经解码过程，通过统一序列生成与奖励估计，并引入约束感知奖励剪枝技术，在满足严格推理延迟要求的同时，实现了平台收益与用户体验的协同优化。实验证明该方法在工业级场景中表现优异。
selection_source: fresh_fetch
motivation: 现有的生成式重排序方法在处理广告信息流的多目标约束时存在推理延迟高且约束处理能力有限的问题。
method: 提出一种将序列生成与奖励估计统一的框架，并利用约束感知奖励剪枝技术在解码过程中直接集成约束条件。
result: 在大规模工业信息流和在线A/B测试中，该方法在满足延迟要求的前提下显著提升了广告收入和用户参与度。
conclusion: 该研究为受限列表优化提供了一种高效的神经生成方案，成功平衡了广告系统的商业收益与用户体验。
---

## 摘要
优化广告信息流中的重排序是一个受约束的组合问题，需要同时实现平台收入最大化和用户体验保持。最近的生成式排序方法通过自回归解码实现了列表级（listwise）优化，但其部署受到高推理延迟和有限约束处理能力的阻碍。我们提出了一种约束感知的生成式重排序框架，将受约束的优化问题转化为有界的神经解码。与以往分离生成器和评估器模型的方法不同，我们的框架将序列生成和奖励估计统一到了单个网络中。我们进一步引入了约束感知的奖励剪枝，将约束满足直接集成到解码过程中，以高效地生成最优序列。在大规模工业信息流上的实验和在线 A/B 测试表明，我们的方法在满足严格延迟要求的同时，提升了收入和用户参与度，为受约束的列表级优化提供了一种高效的神经解决方案。

## Abstract
Optimizing reranking in advertising feeds is a constrained combinatorial problem, requiring simultaneous maximization of platform revenue and preservation of user experience. Recent generative ranking methods enable listwise optimization via autoregressive decoding, but their deployment is hindered by high inference latency and limited constraint handling.   We propose a constraint-aware generative reranking framework that transforms constrained optimization into bounded neural decoding. Unlike prior approaches that separate generator and evaluator models, our framework unifies sequence generation and reward estimation into a single network.   We further introduce constraint-aware reward pruning, integrating constraint satisfaction directly into decoding to efficiently generate optimal sequences. Experiments on large-scale industrial feeds and online A/B tests show that our method improves revenue and user engagement while meeting strict latency requirements, providing an efficient neural solution for constrained listwise optimization.

---

## 论文详细总结（自动生成）

### 论文总结：广告信息流中的约束感知生成式重排序 (CGR)

#### 1. 核心问题与研究动机
*   **核心问题**：在广告信息流场景中，重排序（Reranking）是一个复杂的**受约束组合优化问题**。它需要同时平衡平台收入（广告）和用户体验（自然内容），并满足严格的业务约束（如广告密度、间距、曝光负载等）。
*   **研究动机**：
    *   传统的“生成器-评估器”（GE）两阶段框架存在推理开销大、约束处理与模型解耦的问题。
    *   现有的生成式重排序模型（如自回归解码）虽然能捕捉列表级依赖，但面临**指数级搜索空间**导致的超高延迟，且难以直接在解码过程中嵌入硬性业务约束。

#### 2. 方法论：核心思想与技术细节
该论文提出了 **CGR (Constraint-Aware Generative Re-ranking)** 框架，其核心思想是将受限组合优化转化为**有界神经解码（Bounded Neural Decoding）**。
*   **统一架构**：将序列生成与奖励估计集成在单个网络中。采用多任务列表式架构，利用 **PLE (Progressive Layered Extraction)** 门控机制融合多个专家模块，同时预测曝光（EXP）和点击（CLK）。
*   **分层注意力机制**：
    *   **自注意力专家**：捕捉全局序列交互。
    *   **交叉注意力**：建模用户对位置的偏好。
    *   **局部自注意力 (LSA)**：通过固定窗口掩码捕捉信息流浏览的局部性，减少全局注意力的过度平滑。
*   **有界解码策略**：利用广告数量受限（如每页最多 $K$ 个广告，通常 $K \le 2$）的结构化特征，将搜索空间从 $O(N!)$ 降低到多项式级的 $O(AK)$。
    *   **第一阶段（约束插入）**：在自然内容列表中尝试所有合规的单广告插入位置，选出 Top-1 中间序列。
    *   **第二阶段（有界扩展）**：在中间序列基础上枚举大图广告、双广告等组合，生成所有可行候选列表。
*   **约束感知奖励剪枝**：在解码过程中直接过滤违反业务规则的序列，并利用奖励上界估计提前终止搜索，确保在严格延迟（SLA）内找到全局最优解。

#### 3. 实验设计
*   **数据集**：
    *   **公开基准**：Yahoo! LETOR, Microsoft 10K, Avito, ML1M, KR1K。
    *   **工业数据集**：来自 Bilibili 的大规模广告曝光、点击和收入日志。
*   **对比方法 (Baselines)**：
    *   传统方法：LambdaMART, PRM。
    *   生成式方法：自回归生成排序、非自回归生成排序。
    *   工业基准：生产环境现有的 GE (Generator-Evaluator) 框架。
*   **评价指标**：NDCG@10, MAP, HR@10, RPM (千次展示收入), CTR (点击率), 约束违规率。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **推理性能**：提到了该系统在生产环境中处理每日超过 **10 亿次请求**，且 P99 延迟严格控制在 **40ms 以内**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域的公开数据集和 1 个大规模工业数据集，并进行了为期 14 天的在线 A/B 测试。
*   **充分性**：实验设计较为全面，不仅对比了离线指标（NDCG, AUC），还通过在线测试验证了商业指标（RPM, 会话时长）。
*   **客观性**：通过消融实验验证了各组件（如 LSA, PLE）的有效性，并提供了关于有界解码最优性的理论证明，增强了结果的说服力。

#### 6. 主要结论与发现
*   **性能提升**：在工业数据集上，CGR 相比生产环境的 GE 系统提升了 **11% 的 RPM** 和 **7% 的 CTR**。
*   **在线效果**：在线 A/B 测试显示 RPM 显著增长 **6.8%**，用户会话时长增长 **3.2%**，且约束合规率保持 **100%**。
*   **效率优化**：通过将搜索空间有界化，推理延迟相比全排列搜索降低了 **85% 以上**，证明了在工业级大规模场景下部署生成式重排序的可行性。

#### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了高效的算法，还给出了有界解码在约束集下达到全局最优的理论证明。
*   **架构优雅**：打破了生成与评估分离的传统范式，通过统一模型减少了推理时的重复计算。
*   **约束集成**：巧妙地利用了业务规则（广告数量少）这一“限制”来简化计算复杂度，将约束从“负担”转化为“搜索空间的剪枝工具”。

#### 8. 不足与局限
*   **适用范围受限**：该方法高度依赖于“广告数量 $K$ 较小”这一假设。如果未来广告密度大幅增加，或者约束条件变得极其复杂，有界枚举的复杂度仍可能显著上升。
*   **冷启动与泛化**：论文未详细讨论模型在处理全新广告主或极度稀疏数据时的表现。
*   **硬件细节缺失**：缺乏具体的算力消耗和训练成本数据，对于其他开发者评估迁移成本略显不足。

（完）
